# Test data

`artifact.json` is syntactically valid only as a raw attachment; it is not a
typed critical-evidence envelope. `malformed.json` proves that a matching hash
does not make malformed JSON acceptable.
