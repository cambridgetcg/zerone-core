// Package schedule implements Zerone's durable, consensus-owned schedule
// process state. Node-local pending transaction pools only carry the signed
// create/update/cancel messages until consensus commits them.
package schedule
