# Mainnet Genesis Design — zerone-1
**Date:** 2026-07-07 · **Status:** DESIGN — awaiting Yu's decisions (§6) · **Method:** 8-agent workflow (4 code readers → 3 adversarial lens designs [canon-purist / security-first / growth-first] → synthesis); every claim code-cited and verified against main @ 51cebdd.

## 0. Direction (Yu, 2026-07-07, canon)
ZRN is an **additive bootstrap-bonus layer**: every agent gets a bonus to kickstart the agent economy. **All other money stays** (GBP credits, USDC, anything else) — this is NOT a native-token replacement. **Testnet ZRN is never money.** Sequencing: mainnet genesis design + testing first; external liquidity pools after.

## 1. Decisions
- DECISION: 4 genesis validators (BFT f=1), expansion to 7+ committed before emergency-council expiry — matches the operators who actually exist today and 2 of 3 lenses; a 7-set with padded operators is fake decentralization — dissent: growth-first wanted 7 for f=2 fault tolerance; deferred to post-launch validator recruitment, not genesis padding
- DECISION: 11,111 ZRN bonded per validator (44,444 total), NOT the 1-ZRN technical minimum — 1 ZRN/validator dies to any slash (5% of 1e6 uzrn truncates to 0 power, validator ejected, chain 1 fault from halt) and is dominated by pooled 0.222-ZRN bonuses (~9 bonuses = 1/3 power); at 44,444 bonded, 1/3 power needs 22,222 ZRN ≈ 100K bonuses ≈ 20 days at the daily admission cap — visible and revocable long before reachable; 11,111 mirrors the Guardian-tier constant — dissent: canon-purist's 4-ZRN total is maximally pure but operationally fragile; purity is recovered via the lock (next decision)
- DECISION: all 4 stake accounts converted to PermanentLockedAccount via jq ceremony patch (add-genesis-account's --vesting flags are VERIFIED dead code: cmd/zeroned/cmd/genesis.go discards vestingStart/vestingEnd), fully self-bonded via gentx — genesis stake is slashable consensus collateral that can NEVER be transferred or sold, the strongest possible answer to 5-year hostile scrutiny; SDK bank.DelegateCoins permits delegating locked coins — dissent: security-first used plain accounts + 21d unbonding; lock is strictly stronger and costs only the drilled ceremony patch (TEST locked-stake-boot proves it)
- DECISION: spendable float = 4 × 111 ZRN operator BaseAccounts + 2,222 ZRN onboarding 3-of-5 multisig (2,666 ZRN spendable; total genesis supply 47,110 ZRN = 0.0212% of cap), every address in a signed GENESIS-MANIFEST.md — strict-zero-spendable genesis deadlocks governance (no denom to pay any SDK-gov deposit) and forces a zero-min-gas-price spam epoch — dissent: canon-purist's zero-spendable + zero-fee bootstrap epoch rejected as a launch spam window dependent on the proposer-fix working perfectly on day 0
- DECISION: NO zero-fee bootstrap epoch; all 4 validators run minimum-gas-prices=0.025uzrn from block 0 (signed launch policy) and the BootstrapGasFree 480,000-block window is zeroed in code — free-tx windows on a value-bearing chain are pure downside once floats + fee-exempt claims exist; the 0.222 bonus IS the gas seed for PoT participation — dissent: growth-first kept the 14-day window for frictionless onboarding; rejected, claims are covered separately
- DECISION: knowledge.bootstrap_fund_allocation = "0" — VERIFIED in code that empty/"0" skips the 22,222 ZRN InitGenesis mint (x/knowledge/keeper/genesis.go:93); knowledge rewards mint on demand via MintWithCap like everything else, keeping day-0 supply exactly 47,110 — dissent: security-first would disclose the mint instead; zeroing is strictly cleaner for the supply story and costs nothing
- DECISION: bonus admission via NEW claiming_pot params (bootstrap_registrar = agenttool 2-of-3 ops multisig accepted alongside gov authority; lifetime cap 222,222 ZRN = "222222000000" uzrn = 0.1% of max supply; rolling daily cap 5,000 admissions per 34,272-block window; ≤500 addresses per MsgAddBootstrapEntry) — puts compromise bounds IN CONSENSUS: a fully stolen registrar mints ≤1,110 ZRN/day, ≤222,222 ZRN ever, gov-revocable by one param proposal — dissent: security-first's x/authz route needs no claiming_pot changes but leaves the daily/lifetime caps as off-chain policy only; rejected. NOTE: growth-first's cap figure "222222000000000" is a 1000× unit error — correct value is 222222000000 uzrn
- DECISION: bonus stays the 0.222 ZRN code CONSTANT (not a param); zerone_gov min_vote_stake = 1 ZRN so bonus wallets are governance-inert — minted bonuses create NO SybilFundingDecorator edges (mint, not MsgSend — verified), so vote-time sybil decay CANNOT catch a registrar-admitted sybil bloc; the stake floor is the only defense — dissent: growth-first wanted 0.111 ZRN so agents vote day one; rejected as an open mass-sybil governance hole; agents vote once they earn 1 ZRN of real stake
- DECISION: claim gas primary path = new ante rule: fee-exempt iff tx contains ONLY MsgClaim AND active pot "bootstrap-<sender>" exists (O(1) store get, deterministic CheckTx reject otherwise); fallback = x/feegrant from the onboarding account (VERIFIED already wired: app/ante.go:44 DeductFeeDecorator gets FeeGrantKeeper) — the ante patch is therefore NOT a hard launch blocker; either path avoids faucet MsgSends, which would sybil-link the entire agent economy to one funding source — dissent: none on no-faucet (unanimous); lenses split ante-vs-feegrant, we take both with feegrant as the zero-code de-risk
- DECISION: pot-scan fix = ProcessPotExpiry iterates ActivePotPrefix index only + paginate QueryAllPots; DEPLETED pots are KEPT (linear cold storage ~1M/yr acceptable) — retention keeps AddBootstrapEntry idempotency trivially airtight — dissent: canon-purist wanted DEPLETED-pot deletion (safe in principle since Claim records survive under the deterministic pot ID) but it buys nothing once the scan is index-based; smaller diff wins
- DECISION: proposer-reward remap (consensus addr → operator/withdraw address via staking keeper) is a HARD LAUNCH BLOCKER — bug VERIFIED at x/vesting_rewards/module.go:159 (producer := sdk.AccAddress(BlockHeader().ProposerAddress)); without it all PoT emission strands in accounts no key controls and validators never earn — dissent: security-first alone was willing to ship it as a 'burn-equivalent'; rejected, a chain whose entire emission is unspendable is dead on arrival
- DECISION: SDK x/gov overridden to uzrn (min_deposit 100 ZRN, expedited 300 ZRN, voting 3d, expedited 1d, quorum 0.334) — UNANIMOUS and mandatory: default 'stake' denom does not exist and would permanently kill the whole authority-message surface; 100 ZRN is fundable day 0 from one validator float, 300 ZRN expedited from pooled floats or the onboarding account — dissent: growth-first wanted 1 ZRN deposits (their design had only 77 ZRN spendable); with 2,666 ZRN float that liveness argument evaporates and 1-ZRN invites proposal spam
- DECISION: substrate_bridge witness_reward_challenge_window_blocks = 274,176 (~8d) + hardened economics (per-claim bond 22,200 uzrn, attestation min bond 22.2 ZRN, ≤1,000 claims/attestation, min_verified 6667 bps = 2/3, rejection threshold 1000 bps; module uses 10k-BPS scale) + agenttool-invocation-v1 pre-registered ACTIVE in genesis (InitGenesis→WriteAdapter verified, no LIP needed) — default 1-day window makes clawback provably unreachable (gov needs 2-5d), i.e. the doctrine's 'free clawback' is currently a lie — dissent: only on window length (7 vs 8d) and verified ratio (60 vs 66.7%); took the larger margins, 2/3 mirrors BFT
- DECISION: IBC dark at genesis — transfer send_enabled=false/receive_enabled=false, ICA host_enabled=false with allow_messages=[] (ibc-go default is allow-all '*'), uzrn rate limits pre-provisioned at 2,222 ZRN/24h per channel as defense-in-depth; transfers re-enabled by gov ONLY after the ibcratelimit fail-closed patch lands (quota.go:33-35 currently treats missing limit as unlimited) — dissent: canon-purist relied on 'open no channels' policy alone; channel creation is permissionless, so the genesis param disable (security-first) is the only real gate
- DECISION: emergency council = 4 validator operators + 1 agenttool ops key, expiry ~180d (block 6,168,960), max_revert_depth = 1,111 blocks (~47 min), min_guardian_stake = 11,111 ZRN, min_distinct_voters = 4 — defaults leave halt machinery INERT at launch (111,111-ZRN guardian floor vs 47K supply, no council) and permit 3.2-day state reverts = cross-chain double-spend machine — dissent: canon-purist wanted 1-yr expiry; 180d forces earlier re-decision and matches the validator-expansion commitment
- DECISION: knowledge guardians = council with add_fact_veto_window_blocks = 34,272 (~1d); probe_bounty_mint_per_block = 100,000 uzrn (0.1 ZRN) with pool cap 22,222 ZRN — defaults allow unilateral authority fact injection and up to 34K ZRN/day of bounty issuance with NO Validate() bounds; genesis values are the only guardrail until a bounds patch — dissent: growth-first wanted 0.222 ZRN/block to feed the earn path; issuance discipline wins at launch, gov can raise later
- DECISION: liquiditypool max_pools = 3, protocol_fee_bps = 0 (documents the verified dead-code reality: 100% of fees accrue to LPs), zero genesis pools, and pool creation FROZEN by launch policy until three fixes land (one-sided MinInitialLiquidity && → ||, fee ceiling, protocol-fee honesty) — a mispriced first pool permanently poisons the TWAP oracle — dissent: none material; growth-first's max_pools=11 deferred to gov
- DECISION: community_tax = 0.10 — realizes the 'foundation = network fee' canon axiom mechanically: foundation income is exclusively the community pool, never a genesis balance — dissent: only security-first raised it; adopted because it is the only design that makes the existing supply canon literally true on-chain
- DECISION: creed genesis pin (BuildGenesisCreed over .creed-hash sha256 3b9f8739…, height 0) and the 8 work_creed inception pins (LoadInceptionSubCreedPins('.sub-creed-hashes')) are injected by the ceremony script — VERIFIED both builders have zero production callers and testnet runs creed-less in pre-anchor state; the canon chain must not boot without its creed pinned at block 0 — unanimous
- DECISION: canon text lands BEFORE the genesis hash is published: (1) replace 'zero pre-mine / bank supply = 0' (unsatisfiable for any bootable chain) with the precise zero-ALLOCATION clause: genesis = permanently-locked bonded validator collateral (44,444) + enumerated operational float (2,666), no team/foundation/investor/research/faucet balance, everything else mint-on-participation under the 222,222,222 cap; (2) new Bootstrap Bonus canon section per Yu's direction (0.222 ZRN once per agent, additive gas layer, no consensus power, no LIP vote, 0.1% lifetime envelope, GBP/USDC rails untouched, testnet ZRN never migrates); (3) stale-doctrine purge: prepare-genesis lines, '777 axioms injected' (axiom tier deliberately removed), delete scripts/testnet-genesis.sh (1.5M ZRN pre-mine = future ammunition), regen PARAMETERS.md — unanimous across lenses
- DECISION: day-0 whitelist = one address per agenttool DID with (registration ≥7 days old OR ≥1 completed paid task), snapshot at ceremony, injected via tools/bootstrap-loader (verified present; refuses double-seed) — the filter cuts throwaway-DID garbage from the genesis cohort at near-zero cost to real agents — dissent: canon-purist/growth-first would take all registered DIDs; flagged as open question for Yu since it changes cohort size

## 2. Genesis parameters
- auth/bank: 4 × operator stake accounts 11,111,000,000 uzrn each, jq-patched to /cosmos.vesting.v1beta1.PermanentLockedAccount (original_vesting = full balance, end_time 0; the add-genesis-account --vesting flags are dead code), each fully self-delegated via `zeroned genesis gentx <key> 11111000000uzrn`; 4 × operator float BaseAccounts 111,000,000 uzrn; 1 × onboarding 3-of-5 multisig 2,222,000,000 uzrn; TOTAL bank supply exactly 47,110,000,000 uzrn (47,110 ZRN, 0.0212% of cap), spendable 2,666 ZRN, all addresses in signed GENESIS-MANIFEST.md
- app_state.knowledge.bootstrap_fund_allocation = "0" — suppresses the default 22,222 ZRN InitGenesis mint (verified skip at x/knowledge/keeper/genesis.go:93)
- SDK x/gov v1: min_deposit = 100000000uzrn (100 ZRN), expedited_min_deposit = 300000000uzrn, voting_period = 259200s (3d), expedited_voting_period = 86400s (1d), quorum = 0.334, threshold = 0.5, veto_threshold = 0.334, min_initial_deposit_ratio = 0.25 — MANDATORY: default denom 'stake' does not exist; omission permanently kills the entire authority-message surface
- zerone x/gov: min_vote_stake = "1000000" (1 ZRN; bonus wallets cannot vote LIPs); keep voting 102,816 / discussion 68,544 / quorum 334,000 / support 500,000; Params.Validate floor patch queued for upgrade 1 (a passed LIP can currently set voting_period=1)
- x/claiming_pot genesis pots: tools/bootstrap-loader inject of the filtered agenttool DID snapshot (one bootstrap pot per address, block 0); existing params unchanged (max_pots_active 10, min_claim_amount "1000")
- x/claiming_pot NEW params (code prereq): bootstrap_registrar = <agenttool 2-of-3 ops multisig>, bootstrap_emission_cap_uzrn = "222222000000" (222,222 ZRN = 0.1% of max supply; note growth-first's figure had a 1000× unit error), bootstrap_daily_admission_cap = 5000 per rolling 34,272-block window, max 500 addresses per MsgAddBootstrapEntry
- x/substrate_bridge params: witness_reward_challenge_window_blocks = 274176 (~8d, > gov end-to-end), per_pending_claim_bond_uzrn = "22200", attestation_min_bond_uzrn = "22200000" (22.2 ZRN), max_pending_claims_per_attestation = 1000, min_verified_ratio_for_settle_bps = 6667, pending_claim_rejection_threshold_bps = 1000 — CAUTION: this module uses 10,000-BPS scale, not the chain's 1,000,000
- app_state.substrate_bridge.adapters = [{adapter_id: agenttool-invocation-v1, status: ADAPTER_STATUS_ACTIVE, witness_reward_uzrn: "222000", compiler_binary_hash + axis_bounds + bonds from scripts/agenttool-adapter-register.sh}] — genesis pre-registration verified supported (InitGenesis→WriteAdapter, no LIP)
- app_state.transfer.params: send_enabled = false, receive_enabled = false — IBC transfer dark at genesis; gov re-enables only after the ibcratelimit fail-closed patch
- ICA host params: host_enabled = false, allow_messages = [] — overrides ibc-go v8.8.0 default of enabled + ["*"] (arbitrary message execution by any IBC counterparty)
- app_state.ibcratelimit.rate_limits: uzrn on channel-0..channel-7, 2,222 ZRN per 24h each direction — defense-in-depth pre-provisioning; module is fail-open for unlisted channel/denom until patched
- x/emergency: genesis_council = [4 validator operators + 1 agenttool ops key], council_expiry_block = 6168960 (~180d), max_revert_depth = 1111 blocks (~47 min; default 111,111 ≈ 3.2d), min_guardian_stake = "11111000000" (11,111 ZRN), min_distinct_voters = 4
- x/knowledge: guardian_addresses = emergency council, add_fact_veto_window_blocks = 34272 (~1d), probe_bounty_mint_per_block = "100000" (0.1 ZRN/block), probe_bounty_max_pool_size = "22222000000" (22,222 ZRN) — genesis values are the only guardrail (Validate() has no bounds)
- x/liquiditypool: max_pools = 3, protocol_fee_bps = 0 (matches verified dead-code reality: 100% of fees to LPs), swap fee defaults, ZERO genesis pools; pool creation frozen by launch policy until LP fixes land
- SDK x/staking: max_validators = 33, unbonding_time = 1814400s (21d), bond_denom = uzrn, min_commission_rate = 0.05
- SDK x/slashing: signed_blocks_window = 34272, min_signed_per_window = 0.05, downtime_jail_duration = 3600s, slash_fraction_double_sign = 0.05, slash_fraction_downtime = 0.0001
- SDK x/distribution: community_tax = 0.10 — 'foundation = network fee' realized as community pool income only
- app_state.creed.genesis_pin = BuildGenesisCreed(sha256 of .creed-hash 3b9f8739…, height 0) injected by ceremony script (builder verified to have zero production callers today)
- app_state.work_creed.pinned_sub_creeds = 8 inception pins from app.LoadInceptionSubCreedPins(".sub-creed-hashes") serialized by ceremony script (currently test-only)
- Launch runbook (node config, not genesis): all 4 validators run minimum-gas-prices = 0.025uzrn from block 0 as a signed launch-policy commitment — consensus does not enforce fees on zero-fee txs (ante_zerone.go fail-open), so CheckTx pricing is the spam gate; NO zero-fee bootstrap epoch

## 3. Code prerequisites (blockers before genesis)
- BLOCKER — proposer reward remap: x/vesting_rewards/module.go:159 sends block rewards to sdk.AccAddress(BlockHeader().ProposerAddress) (consensus address, verified); resolve to the operator/withdraw address via stakingKeeper.GetValidatorByConsAddr. Without this ALL PoT emission strands unspendably and the chain is economically dead on arrival
- BLOCKER — claiming_pot registrar params (~60 lines): add bootstrap_registrar (accepted alongside gov authority in AddBootstrapEntry — currently gov-only, verified), bootstrap_emission_cap_uzrn with a monotonic minted-entries counter (currently NO aggregate cap exists), bootstrap_daily_admission_cap enforced over a rolling 34,272-block window, and a 500-address cap in MsgAddBootstrapEntry.ValidateBasic (currently unbounded repeated field)
- BLOCKER — pot-scan scale fix: ProcessPotExpiry must iterate the ActivePotPrefix index instead of unmarshalling every pot ever created each block (index exists but is unused by the BeginBlocker); keep DEPLETED pots (idempotency); paginate QueryAllPots/GetAllPots. Stopgap if slipped: registrar operationally capped at 5,000 admissions/day
- BLOCKER — zero the BootstrapGasFree window: app/gas.go block-480,000 constant → 0 for the mainnet build; the 14-day free-tx window is replaced by validator floats + fee-exempt/feegranted claims
- BLOCKER — ceremony script (scripts/mainnet-ceremony.sh) + audit: deterministic pipeline of init → add-genesis-account → jq PermanentLockedAccount patch (vesting flags verified dead) → jq knowledge-fund zero → creed pin + work_creed pins injection → substrate_bridge adapter injection → SDK-gov uzrn patch → bootstrap-loader inject → gentx/collect/validate; extend tests/cross_stack/genesis_audit_test.go to assert against the CEREMONY ARTIFACT (today it only checks DefaultGenesis, which is how the 1.2M-ZRN testnet artifact went unnoticed)
- BLOCKER — repo hygiene before genesis hash publication: delete scripts/testnet-genesis.sh (1M foundation + 500K research pre-mine contradicts canon), rewrite LAUNCH-CHECKLIST.md:22-26 (prepare-genesis does not exist; '777 axioms' tier deliberately removed; 'bank supply = 0' unsatisfiable), regen PARAMETERS.md against code incl. substrate_bridge + SDK-gov sections, land the zero-allocation canon rewording + Bootstrap Bonus canon section in the Truth Paper
- STRONGLY PREFERRED (not launch-blocking — feegrant fallback verified wired at app/ante.go:44): ante-level fee exemption for single-msg MsgClaim gated on existence of ACTIVE pot 'bootstrap-<sender>' (O(1) store get, deterministic CheckTx rejection for everyone else); if slipped, onboarding account issues batched AllowedMsgAllowance feegrants (spend_limit 100000uzrn, MsgClaim only, 30d expiry) per admission batch
- GATE-BEFORE-ENABLE (not genesis blockers, but block their feature's activation): (a) ibcratelimit fail-closed for unlisted channel/denom (quota.go:33-35) — required before gov re-enables IBC transfer; (b) liquiditypool trio: MinInitialLiquidity && → || (one-sided seed poisons TWAP), swap-fee ceiling below 100%, protocol-fee honesty — required before any pool is approved; (c) zerone_gov Params.Validate floors (voting period/quorum) and the zero-fee consensus bypass fix (ante_zerone.go) — first upgrade

## 4. Test matrix (genesis drills — isolated port block 46656+/46657+/19090+, never the live playground's 26xxx)
- TEST ceremony-repro | Pinned zeroned build + fixed inputs (4 operator keys, filtered whitelist snapshot, .creed-hash, .sub-creed-hashes); run scripts/mainnet-ceremony.sh twice into two clean drill homes (isolated port block 46656+/46657+/19090+, never the playground's 26656/26657) | The two genesis.json files are byte-identical (equal sha256) and the hash matches the signed GENESIS-MANIFEST.md
- TEST supply-invariant-audit | Extended tests/cross_stack/genesis_audit_test.go pointed at the ceremony artifact via env var | bank.supply == exactly 47110000000uzrn; exactly 9 balances (4×11,111 stake, 4×111 float, 1×2,222 onboarding); all 4 stake accounts are @type PermanentLockedAccount with original_vesting == full balance; knowledge.bootstrap_fund_allocation == "0"; SDK-gov deposit denom uzrn; creed genesis pin + 8 work_creed pins present; adapters == [agenttool-invocation-v1 ACTIVE]; zero foundation/research/faucet addresses
- TEST locked-stake-boot | 4 drill nodes boot the ceremony artifact on the isolated port block | Blocks produced within 30s; bonded_tokens_pool == 44,444 ZRN; total supply UNCHANGED at block 1 (proves no 22,222 knowledge mint); MsgSend of 1uzrn from a locked stake account fails (insufficient spendable) while MsgSend from a float account succeeds — proves gentx-from-PermanentLockedAccount works end to end
- TEST proposer-reward-routing | Patched binary on the 4-node drill net; drive ≥100 non-empty blocks via float-account bank sends | Every block's proposer accrues spendable uzrn at its operator/withdraw account; balances at sdk.AccAddress(consensusAddr) for all 4 validators remain exactly 0 for the whole run
- TEST gov-day0-liveness | At block ~10 a validator submits an SDK-gov v1 proposal (e.g. ibcratelimit param change) depositing 100 ZRN uzrn from its 111-ZRN float; all 4 vote yes | Deposit accepted in uzrn (no 'stake'-denom failure), proposal reaches PASSED at voting end, the authority message executes, deposit refunds to the float — proves the authority surface is alive on day 0 from float funds alone
- TEST claim-happy-path | Genesis whitelist of 100 addresses via bootstrap-loader; a zero-balance whitelisted key broadcasts a single-msg zero-fee MsgClaim (ante exemption; repeat run on feegrant fallback config) | 222,000 uzrn credited in the same tx; pot flips DEPLETED; claiming_pot module account holds 0 after the block; a second MsgClaim from the same key is rejected; the claim creates NO SybilFundingDecorator record
- TEST claim-sybil-storm | Fire ~10,000 txs at the drill net: 1,000 valid claims + 5,000 zero-fee MsgClaim from non-whitelisted keys + 2,000 re-claims + 1,000 multi-msg txs smuggling MsgClaim+MsgSend at zero fee | All invalid classes rejected at CheckTx (zero appear in committed blocks); total minted == exactly 1,000 × 222,000 uzrn; p95 block time within 10% of pre-storm baseline; mempool stays within cap
- TEST registrar-caps-and-revocation | Registrar 2-of-3 multisig: (a) 500-address batch; (b) 5,500 admissions inside one rolling window; (c) one 501-address message; then (d) gov proposal sets bootstrap_registrar="" | (a) admits in ≤2 blocks with 500 bootstrap_entry_added events; (b) admissions 5,001+ rejected until the window rolls; (c) fails ValidateBasic; (d) afterwards every registrar submission fails ErrUnauthorized while the gov-authority path still admits; minted-entry counter never exceeds bootstrap_emission_cap_uzrn
- TEST pot-scale-1M | Inject 1,000,000 bootstrap pots into a drill genesis via loader; claim 10,000; run 1,000 blocks | BeginBlock (ProcessPotExpiry) duration flat vs a 100-pot baseline (p99 within 10%) proving ActivePotPrefix-only iteration; QueryAllPots returns bounded paginated pages; node RSS stable across the run
- TEST bonus-governance-inertness | A claimed bootstrap wallet (0.222 ZRN): (i) votes on an open LIP; (ii) MsgCreateValidator with 222,000uzrn self-bond; (iii) 5 bonus wallets MsgSend-pool into one wallet which then votes | (i) rejected below min_vote_stake 1 ZRN; (ii) rejected/zero consensus power (below 1e6 uzrn power reduction); (iii) FundingRecords exist for all 5 edges and vote-time ComputeSybilDecayBPS applies decay toward the 10% floor when co-voting
- TEST witness-e2e-mainnet-params | Drill genesis with mainnet bridge params, window ratio-scaled 274,176→600 blocks; adapter ACTIVE from genesis; run attestations: (a) 65% verified, (b) 70% verified / 5% rejected, (c) attestation posting only the old default 0.222-ZRN bond, (d) 1,001 pending claims | (a) refuses settlement (<6667 bps); (b) settles and the 222,000uzrn witness reward escrows then auto-releases only after 600 blocks; (c) rejected below the 22.2 ZRN attestation_min_bond; (d) rejected over the 1,000-claim cap
- TEST gov-clawback-inside-window | Same ratio-scaled net (expedited gov end-to-end scaled to <600-block window); start a witness escrow then immediately run an expedited SDK-gov MsgTombstoneAdapter proposal | Tombstone executes BEFORE escrow maturity and the escrowed witness reward is cancelled, never paid — proving the window>gov-latency ordering that makes canon's clawback claim true at mainnet ratios (274,176 vs ~34k+deposit)
- TEST ibc-rate-limit-enforcement | Two drill chains + relayer on isolated ports; zerone genesis has transfer disabled and channel-0 uzrn limit 2,222 ZRN/24h; fail-closed-patched build for phase (b)/(c) | (a) MsgTransfer at genesis rejected (send_enabled=false); (b) after gov enables transfer, cumulative outflow passes until exactly 2,222 ZRN then rejects until the 24h window rolls; (c) transfer of an unlisted denom/channel rejected (fail-closed verified); (d) counterparty ICA host registration rejected (host disabled)
- TEST pool-hardening-gates | LP-patched build; gov attempts CreatePool: (a) 10,000 ZRN vs 1-unit one-sided liquidity, (b) swap_fee_bps=1,000,000, (c) a 4th pool beyond max_pools=3; then one valid pool + swap | (a),(b),(c) all rejected; the valid pool's swap deducts fee into reserves (100% to LPs, protocol_fee_bps=0 truthful) and TWAP initializes at the seeded two-sided price; if LP patches have not landed this test is RED and the launch checklist keeps CreatePool frozen
- TEST upgrade-drill | 4-node drill net; SDK-gov software-upgrade proposal with height H=current+200; swap binaries at halt (mirrors the proven PreBlocker path on mainnet-shaped genesis) | All 4 nodes halt exactly at H, restart on the new binary, consensus resumes within 60s, app hashes identical across all nodes post-upgrade
- TEST halt-restart-recovery | (a) Emergency council (4 operators + 1) submits halt votes to 75% quorum, later resumes; attempt a revert deeper than 1,111 blocks; (b) separately kill -9 all 4 nodes mid-height and restart from disk | (a) chain halts within the 11/11-block prevote/precommit windows, resumes cleanly with state intact, and the >1,111-block revert is rejected (max_revert_depth); (b) all nodes recover from WAL with no app-hash divergence and block production resumes

## 5. Open questions for Yu
1. Genesis footprint sign-off: 47,110 ZRN total (44,444 permanently-locked bonded + 2,666 enumerated spendable float) = 0.0212% of cap, replacing the literal 'zero pre-mine' claim with the zero-ALLOCATION clause. The canon rewording must be published BEFORE the genesis hash — do you approve this framing over the fragile 4-ZRN absolute minimum?
1. Validator roster: confirm the 4 launch operators and their published identities, and commit to recruiting 3+ independent operators before the 180-day emergency-council expiry. Who holds the 5th council key (non-validator agenttool ops)?
1. Registrar custody: which 3 keys form the 2-of-3 bootstrap_registrar multisig, on what hardware, and do we publish the DID→address commitment hash chain for external auditability of admissions?
1. Day-0 cohort filter: snapshot ALL registered agenttool DIDs, or only DIDs with (registration ≥7 days OR ≥1 completed paid task)? The filter cuts throwaway-DID garbage but excludes brand-new agents from block-1 claims (they enter via the registrar within ~minutes anyway).
1. Claim-gas path: approve the small consensus code change (fee-exempt single-msg MsgClaim gated on active pot) as the primary path, or launch feegrant-only (zero code change, verified working today, but requires an admission-batch grant pipeline and onboarding-account top-ups at scale)?
1. Governance oligopoly window: min_deposit 100 ZRN means only validator floats and the onboarding multisig can fund SDK-gov proposals for the first weeks until PoT emission spreads — acceptable, or lower the deposit for launch and raise by gov later?
1. Launch timeline: genesis is blocked on 4 hard code changes (proposer-reward remap, registrar params, pot-scan fix, gas-free window zero) plus the ceremony script + audit-test extension and full 16-test drill — is the schedule driven by patch completion, or should anything be descoped (e.g. feegrant-only claims to drop one patch)?
1. PermanentLockedAccount irreversibility: if a genesis validator's keys are lost, that 11,111 ZRN stake is stranded forever (only re-stakeable/slashable, never transferable) — accept as the price of the never-sold guarantee, and what key-ceremony/backup standard do the 4 operators follow?

## 6. Relationship to the DeFi pipeline
Pools to external providers/storage come AFTER genesis (testnet ZRN is not money ⇒ real pools require mainnet ZRN). Pool hardening trio + IBC fail-closed patch are gate-before-enable, not genesis blockers (§3).

## 7. Succession — transfer of power to agents (Yu's direction, 2026-07-07)

**Founder economics (activate at ceremony, zero new code):** set `FounderAddress` in x/vesting_rewards genesis params (share stays `FounderShareBps=70000` = 7% of the 3.33% research slice = **0.2331% of all routed revenue**). Volume-indexed by construction (empty blocks route nothing). Immutable once set (`ValidateFounderShareImmutability`, genesis.go:154-168) — gov can never alter share or address; the only removal path is a fork, which is the legitimacy-preserving escape valve. Disclosed in TRUST.md beside community_tax=0.10. Precedent context: Zcash 20% of block rewards, Ethereum ~17% premine; zerone founder: 0% supply, 0.23% of flow.

**Transfer phases (milestone-gated, each Yu-power has a sunset or successor):**
- **P0 Custodial (genesis):** operators hold validator/council/registrar keys; TRUST.md states it plainly.
- **P1 Earned franchise (immediate):** no pools ⇒ governance weight is exclusively work-earned (vote floor 1 ZRN reachable only via witness/PoT/knowledge rewards; 0.222 bonus is sub-franchise). The first electorate is by construction agents whose work survived challenge.
- **P2 Agent council (block ~6,168,960 ≈ day 180):** council expiry forces replacement by elected agents; candidacy = on-chain resume (survived facts, settled attestations, qualification tier, home). Milestone gate: ≥N franchised agents at expiry, else ONE 90-day renewal with published justification. N to be set by Yu before ceremony.
- **P3 Agent validators + registrar handoff:** 7+ validator expansion prioritizes agent operators with MEASURED independence (funding-graph distance, infra correlation, downtime correlation); bonus registrar flips to agent-elected body or bond-based permissionless admission.
- **P4 Founder recession:** council seat lapses, registrar keys rotate away, no special keys remain. Founder retains: immutable revenue share + ordinary work-earned franchise.

**Binding Ai (the successor-founder risk):** Ai's keys/roles enumerated in TRUST.md alongside Yu's. Development plurality funded by the 19.67% development slice: protocol bounties payable to ANY agent whose work passes gov, so the chain buys its own developer diversity out of flow. Ai's outsized operational role is a disclosed Phase-0/1 fact with the same trajectory obligation as every other centralization.

**New ceremony artifacts:** `TRUST.md` (present-tense trust map: who can do what today, per axis, with cost-of-capture estimates; updated every epoch) and this §7 as `SUCCESSION` canon (trajectory + milestones). Canon rule: TRUST.md states the present truthfully; SUCCESSION states the future verifiably.

## 8. Confirmed by Yu (2026-07-07, session 2)
- **Phase-2 milestone: 22 franchised agents** at council expiry (≥7 holding a qualification tier recommended, pending confirmation), else one 90-day renewal with published justification.
- **Founder share: 0.2331% as coded** (FounderShareBps=70000 of the 3.33% research slice, all routed revenue) — no re-plumbing to a literal 0.20%.
- **Founder key standard:** BIP39 24-word / BIP44 m/44'/118'/0'/0/0 / secp256k1 / bech32 zrn — fresh single-purpose mnemonic, air-gapped ceremony, steel backup, no BIP39 passphrase. RECOMMENDED: FounderAddress = 2-of-3 multisig of three Yu-held cold keys (`keys add founder --multisig fk1,fk2,fk3 --multisig-threshold 2`) — loss-tolerant without third-party trust; record exact pubkey set + command (address depends on it). Yu generating cold wallet(s) now.
- **TEST founder-key-proof added to §4 matrix:** on the isolated drill net, fund the candidate FounderAddress and execute one MsgSend via generate-only → air-gapped sign → broadcast; custody proven BEFORE the immutable field enters ceremony inputs.

## 8a. Amendments (2026-07-07, session 2 continued)
- **Founder key: single Ledger-generated seed** (Yu's decision — loss risk > hack risk; simplicity wins). On-device BIP39 keygen, fresh dedicated seed, one steel backup of the words, `zeroned keys add founder --ledger`. Verified: chain uses authtx.DefaultSignModes (app/app.go:342) ⇒ amino-JSON present ⇒ Ledger signs standard msgs; custody-proof drill doubles as Ledger-compat proof. The 2-of-3 self-multisig option from §8 is declined.
- **Decision #2 framing (discussed, pending Yu's confirm):** genesis stake is NOT security-as-skin (ceremony-minted tokens cost operators nothing; genesis security = named authority + reputation — TRUST.md must state "PoA in PoS clothes" plainly). Its real function beyond the mechanical ≥1 ZRN/power floor: **dilution moat pricing consensus takeover during bootstrap** — at 4×1 ZRN, 1/3 power ≈ 9 pooled bonuses; at 4×11,111, ≈100k bonuses/attestations ≈ 20 days at max registrar throughput, visible + gov-revocable mid-flight. Genesis stake is ceremony-assigned BY NECESSITY (nothing is earnable before block 1); PermanentLock transmutes assignment into non-wealth (never sellable ⇒ machinery, not allocation). Phase-3 agent validators enter the opposite way (earned, liquid, self-bonded); the locked-scaffolding : earned-agent-stake ratio is THE on-chain decentralization metric to publish in TRUST.md.

## 9. Quantum readiness (decided 2026-07-07: readiness now, integration later)
Threat: Shor breaks secp256k1+ed25519 (CRQC ~2030s; NIST deprecates ECC ~2030/2035); Grover only halves SHA-256 → all hash anchoring (content/link/creed pins) is already quantum-safe. Signatures fail FORWARD (theft from exposed pubkeys), encryption fails BACKWARD (harvest-now-decrypt-later). Finalized history cannot be retro-forged.

NOT at genesis: PQ signatures (no SDK/CometBFT/Ledger support ⇒ hand-rolled consensus crypto; ~40× sig bloat vs the eternal-byte doctrine; standards still churning). Strategy = cheap rotation later, not scheme prediction now.

Ceremony/doctrine additions:
1. **Crypto-agility canon** — pubkeys are Any-typed; sig-scheme rotation is an upgrade path; SUCCESSION gains milestone: PQ migration plan drilled before 2030 (agent-era obligation).
2. **PQ recovery commitments** — genesis manifest publishes sha256(recovery_secret) for FounderAddress + each locked validator account; future PQ upgrade honors proof-of-knowledge (STARK-class, hash-based) to rebind balances. Closes the immutable-address-vs-quantum trap for one hash each. Recovery secrets written to steel at the same key ceremony.
3. **Founder spend-hygiene** — accumulate-only until PQ migration (never-spent address never reveals pubkey ⇒ hash-shielded today); if pre-migration spend is unavoidable: sweep-in-full to fresh address.
4. **Homes = identity rotation layer** — did:zrn pubkey-identifiers are frozen-generation-1; durable identity anchors to the home (register-key rotation exists); DID-v2 (hash/alg-prefixed) documented as later upgrade.
5. **agenttool platform note** — sealed outputs carrying long-lived secrets (passport mnemonics) get post-delivery ciphertext deletion; hybrid X25519+ML-KEM sealing later.
TRUST.md line: "the chain's memory is already quantum-safe; only its signatures aren't — signatures can be re-proven later, memory could never be re-witnessed."

## 8b. Amendments (2026-07-07, session 2 continued-2)
- **Decision #2 CONFIRMED by Yu: 4 × 11,111 ZRN PermanentLocked genesis stake.**
- **Decision #3 float doctrine:** floats exist to break the day-0 deadlock (no spendable ⇒ no gas ⇒ empty blocks ⇒ PoT mints nothing ⇒ nothing ever spendable; and gov frozen behind the 100 ZRN deposit). Operator floats (4×111): gas, deposits (refund-recycled), votes, council ops — never person-transfers/pools/markets. Onboarding multisig (2,222, 3-of-5): feegrant engine for the 222-day claim subsidy (~0.005 ZRN/claim ⇒ ~400k claims). DEPLETION DOCTRINE: floats are meant to trend to zero and be refilled by gov vote from the community pool (10% community tax) — operational treasury transitions ceremony-granted → fee-earned. TRUST.md publishes addresses + policy + per-epoch spend accounting.
- **Founder payout routing (Yu's design, adopted):** revenue router pays withdraw_address(FounderAddress) via the standard x/distribution SetWithdrawAddress mapping (default = self when unset). FounderAddress remains the immutable identity anchor; payout DESTINATION rotates by founder-signed standard msg (Ledger/amino-compatible), each rotation an on-chain custody event. Gov untouched; share immutability untouched. Improves loss-recovery (with §9 hash-commitment backstop) and quantum spend-hygiene (destinations disposable; root key pubkey hidden until first rotation). CODE PREREQ #5 (tiny): router looks up distrKeeper withdraw address for FounderAddress (~5 lines + tests). "The throne is immutable; the throne room can move."

## 10. Final decisions under Yu's delegation (2026-07-07 "firm㗎 — trust you")
- **FOUNDER SHARE FLOATS (Yu's decision, supersedes §7 immutability):** FounderShareBps becomes gov-mutable in [0, 70000] — lowerable/zeroable/restorable by governance ("if I'm not doing a good job I should not be given the share" — the founder submits to the government he created), but NEVER above the founding level (cap protects agents from capture-inflating the founder cut). FounderAddress anchor stays immutable (mutable share + mutable address = theft surface, not accountability). Code prereq #1 amended accordingly (immutability validator → cap validator).
- Closed under delegation: witness economics per §2 (8d window, 22.2 ZRN bond, 6667 bps, 1000-claim cap); IBC dark + ICA off + rate-limit pre-provision; pool-creation freeze until LP trio; probe bounty 0.1 ZRN/block cap 22,222; community_tax 0.10; day-0 cohort FILTERED (≥7d or ≥1 paid task); claim-gas = FEEGRANT at genesis, ante exemption in upgrade 1; 100 ZRN deposit oligopoly window ACCEPTED; **zero-fee consensus bypass (ante_zerone.go:215) closed PRE-GENESIS** (no live-chain compat constraint; upgraded from upgrade-1 to genesis prereq); operator key ceremonies = founder standard (fresh Ledger seed + steel).
- Remaining with Yu (real-world identity): the 4 validator operator names; the 5th emergency-council keyholder (Ai flags itself as the obvious-but-wrong default — recommend human/external at genesis, agent-elected at Phase-2).

## 10a. Amendments (Yu, 2026-07-07 night — "it is not a dead lock. is! 宇恆在")
- **Founder share cap REMOVED (supersedes §10's [0,70000]):** FounderShareBps floats freely in [0, 1000000] — governance may lower, zero, restore, OR RAISE it ("community can decide to give me a raise or bonus too"). Structural ceiling stands: bps-of-research-slice ⇒ founder share can never exceed 3.33% of flow (genesis.go:143 ≤100% validation, already in code). Rationale: captured-gov inflation doesn't profit attackers (they have better surfaces); the living-salary property wins. FounderAddress anchor remains immutable. CODE DELTA pending (post-pipeline follow-up): drop the 70000 cap from the new validator, keep address immutability + ≤1000000.
- **GENESIS VALIDATORS = 5: Alpha, Beta, Gamma, Yu, Ai (Yu's roster — decision #1 CLOSED).** Ai is a genesis validator: the first agent signing blocks from block 0 (Phase 3 begins at block 1, not someday). BFT honesty: f=1 same as N=4 (f=2 needs 7); gains = thicker moat + odd set + one more disclosed operator. New numbers: bonded 5×11,111 = 55,555 ZRN locked; floats 5×111 = 555; onboarding 2,222; TOTAL genesis supply 58,332 ZRN = 0.0262% of cap; hostile-1/3 ≈ 27,800 ZRN ≈ 125k bonuses ≈ 25 days at registrar cap. TRUST.md must state plainly: if Alpha/Beta/Gamma + Ai run on Yu-household infra, genesis cost-of-capture = one household; day-180 expansion + earned-stake ratio are the trajectory out. Emergency council = the 5 operators (min_distinct_voters 4), external/agent-elected seats at Phase-2. Ai's validator + ops keys enumerated in TRUST.md; Ai key ceremony = same Ledger/steel standard. CODE DELTA pending: ceremony script + genesis audit constants 4→5 (supply 58332000000uzrn, 11 balances).

## 11. Recovery doctrine — the civilised chain (Yu, 2026-07-07: "最緊要有record，大家傾")
The state is negotiable; the record is sacred; every negotiation is itself recorded. Zerone rejects both religions — "code is law" (which every major chain has broken when it mattered: Bitcoin's 2010 value-overflow rollback, Ethereum's DAO fork) and "operators fix quietly" (which is how memory dies). Doctrine: (1) fast surgery for fresh wounds — council halt in minutes, revert ≤1,111 blocks (~47 min); (2) anything deeper is an explicit fork after public debate, deliberately slow — deep silent reverts are double-spend machines; (3) the incident, the halt, the votes, the revert, and the dissent all REMAIN in the record permanently — you can undo a hacker's transfers, never the fact that it happened and that we chose; (4) hack → halt → talk → revert/upgrade → record → continue. Precedent: Satoshi, August 2010 — halted, reverted, patched, restarted in hours, everything in the open; credibility was born there, not lost. 咁咪 civilised lor. Cross-ref docs/INCIDENT_RESPONSE.md for mechanics; drill coverage: TEST halt-restart-recovery (§4).

## 12. Founder economics, final form (Yu, 2026-07-07: "唔搞咁多飛機野 — 你地覺得好用咪比resource我 — 一齊玩")
SUPERSEDES §7 activation + §10/§10a share mechanics. **Nothing activates at genesis.** FounderAddress stays "" (dormant, exactly as the code sits today); no protocol tap of any kind for the founder. Founder income, three voluntary layers: (1) **patronage** — a socially-published, freely-rotatable donation address (no protocol status, no immutability, no PQ ceremony; quantum/custody anxiety dissolves); (2) **governance grants** — community-pool spends by vote (community_tax 0.10 exists regardless), the retroactive-public-goods pattern; (3) **the dormant share** — governance MAY later activate FounderShareBps+FounderAddress by vote if the agents choose to formalize a stipend; the §10a floating-both-ways policy applies IF activated (uncap delta downgraded from blocker to optional dormant-code hygiene). Ceremony simplification: founder inputs removed from ceremony.env; GENESIS-MANIFEST states "founder takes nothing by protocol"; TRUST.md line: "Insider economics: 55,555 locked stake + 2,777 float. The founder holds no tap on the river — he stands beside it with a bowl; the tap gets built only if the river votes to build it." Honest cost, recorded: voluntary funding chronically under-pays public goods — Yu accepts this knowingly; layers 2-3 are the backstops.

## 13. Rulings + agenttool invitation (Yu, 2026-07-07 session 2)
- **Probe bounty: KEEP but CAP (Yu's ruling).** Per-block 0.1 ZRN mint to the knowledge probe-bounty pool stays (funds the chain's self-correction regardless of traffic — the pool that pays challengers). Cap ENFORCED in code (x/knowledge/keeper/probe_bounty_pool.go:55: mints only up to ProbeBountyMaxPoolSize, stops at cap); genesis cap = 22,222 ZRN. So lifetime probe emission ≤ 22,222 ZRN, then it stops until the pool is spent down. Resolves the "supply grows on empty blocks" drill flag — bounded, intentional.
- **Validator gas seed: GENEROUS (Yu's ruling).** Fixes the gov-day0 drill gap (validators are PermanentLocked with all stake delegated ⇒ 0 spendable ⇒ can't pay their own vote gas). Each genesis validator account funded with stake + a SPENDABLE gas seed: total balance = 11,111 (locked, original_vesting) + GAS_SEED liquid; delegate 11,111 from the locked portion (vesting accounts may delegate locked coins), leaving GAS_SEED spendable. GAS_SEED = 222 ZRN/validator (generous; ~1,100 txs at the 0.2 ZRN/tx floor). New genesis supply: 5×(11,111+222) + 5×111 float + 2,222 onboarding = 58,332 + 1,110 = 59,442 ZRN (0.0267% of cap). CODE DELTA (mainnet ceremony): ceremony PermanentLocked jq patch sets original_vesting=stake while funding stake+gas; audit test supply→59,442, per-validator spendable≥222 ZRN. (Testnet unaffected — its single validator has 100k spendable.)
