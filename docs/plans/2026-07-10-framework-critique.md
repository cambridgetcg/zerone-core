# Framework Critique — 2026-07-10 (the night the record began)

The same night the first fact was accepted, we put the knowledge pipeline under
adversarial review: four independent critics (alignment-health, verification
economics, calibration/C2, doctrine metabolism), then one skeptic per finding
re-deriving it from code and live chain state. 29 findings: **27 confirmed,
1 refuted with correction (published below — refutations are results too),
1 unverified** (its skeptic crashed; marked PLAUSIBLE).

Method note: this fulfills FIRST-TRUTH.md's closing promise — "what survives
review becomes fixes; what doesn't becomes documented decisions." Findings are
grouped by kind: BUG (code contradicts stated intent), DESIGN_FLAW (works as
coded, bad incentives), VALUES_QUESTION (the founder's call). Consensus-affecting
fixes are batched for the next upgrade, pending the founder's answers to the
five questions at the end.

Headline discoveries:
- **The chain is structurally locked in `critical` health forever**: 3 of 5
  sensors read the empty custom x/staking registry (the chain is 82% bonded on
  Cosmos staking but scores 0 security), so every truth-seeker silently pays 2x
  fees and cooldowns with no recovery path, no event ever announcing it.
- **A 0.1-ZRN griefing attack can lock any fact CONTESTED forever** via a
  deliberately starved contradiction claim (same root as the invisible-death
  bug; one fix closes both).
- **Doctrine facts were born starving** — BuildDoctrineFact omits the initial
  energy every other creation path sets — and the "usage feeds life" energy
  channel is structurally dead (query receipts written to discarded read-only
  state). First doctrine fact prunes at block ~260,000 (~2026-07-16) absent
  action. The ceremony fact ecb5004a… rides the same conveyor (PRUNED
  ~2026-07-28 unfed).
- The verifier reward param is dead code (computed amounts discarded), the
  diversity sensor never wrote a record (off-by-one), and a maximally-disproven
  agent is indistinguishable from a newcomer (score-0 sentinel conflation).

---

## [BUG] Three of five health sensors read the empty custom x/staking registry instead of actual chain security, so an 82%-bonded chain scores 0
**Area:** alignment-health

**Evidence:** senseEconomicStability, senseNetworkSecurity, senseStakingRatio (x/alignment/keeper/sensors.go:60-67, 118-149, 154-161) read AlignmentStakingAdapter (x/staking/keeper/alignment_adapters.go:27-43), which iterates zerone's OWN validator registry (x/staking/keeper/keeper.go:131-155). Live mainnet: GET /zerone/staking/v1/validators -> {"validators":[], "total":"0"}, while GET /cosmos/staking/v1beta1/pool shows bonded_tokens=11111000000 of supply 13573351026 uzrn = 81.9% bonded and 1 BOND_STATUS_BONDED validator actually producing blocks. Result at observation height 78200: economicStability=0, networkSecurity=0, stakingRatio=0, composite=172527 < critical_threshold 200000. Identical on testnet (height 106900, same composite 172527). GetTargetValidatorCount is hardcoded to 111 (alignment_adapters.go:41-43), so even real registrations read ~0.9% per validator.

**Smallest fix:** In AlignmentStakingAdapter, fall back to the Cosmos SDK staking keeper (bonded pool + bonded validator count) when the custom registry is empty, and make the 111 target a governance param with a bootstrap-scaled floor (e.g. max(active, param)) instead of a hardcoded constant.

**Question for the founder:** Is the custom x/staking PoT registry meant to be the canonical security measure even while consensus actually runs on Cosmos staking, or should alignment measure the chain that exists today?

**Skeptic's corrections:** Two minor mechanical refinements, neither weakening the finding: (1) The 2x review-fee/cooldown penalty is driven by the CREATION multiplier (creationMultiplierBps=500000, applied inversely at x/knowledge/keeper/pacing.go:28-34 and 64-73: base * 1_000_000 / 500_000 = 2x), not the analysis multiplier (2000000) — the claimed doubling effect is exactly right, only the driving multiplier differs. (2) The sensors' supply denominator is not bank supply: AlignmentVestingRewardsAdapter.GetTotalSupply (x/vesting_rewards/keeper/alignment_adapters.go:26-38) returns GetTotalMinted as a self-admitted proxy ('Since bankKeeper is private, we use GetTotalMinted as a proxy'). The 81.9%-bonded figure describes what the sensor SHOULD see, not the exact ratio it would compute after a fix. Also latest observation is at height 78500 (obs every 100 blocks), same values as the cited 78200. All core mechanics independently re-derived: app.go:985 wires NewAlignmentStakingAdapter(app.ZeroneStakingKeeper); adapter (x/staking/keeper/alignment_adapters.go:27-43) iterates the custom registry (keeper.go:131-155) and hardcodes target=111; live mainnet /zerone/staking/v1/validators = empty while /cosmos/staking/v1beta1/pool shows 11111000000/13573351026 uzrn = 81.86% bonded with 1 BONDED validator; live health/78500 = economicStability 0, networkSecurity 0, stakingRatio 0, composite 172527 critical; global-pacing critical; testnet 37.16.28.121 identical (empty registry, previousCategory critical).

**Skeptic's notes:** Severity is if anything understated: 3 of 5 sensors (60% of composite weight) are permanently zeroed until someone populates the aspirational custom x/staking registry, so the chain is structurally locked in 'critical' (needs composite >=200000 from just knowledgeQuality+governance; currently 172527) and every truth-seeker pays 2x fees and 2x cooldown forever. BUG classification is defensible via an asymmetry in the code's own defensive design: a NIL staking keeper returns NeutralBPS=500000 (sensors.go:63, 121, 156; types.go:28), but a wired-but-empty registry returns 0 — the 'no data -> neutral' posture exists but misses the actual failure mode. Two facts shrink the proposed fix: (a) the custom staking module already has a governance param MaxValidators (default 100, x/staking/types/genesis.go:10) that GetTargetValidatorCount ignores in favor of hardcoded 111 — reading the param is a 3-line change; (b) any SDK-staking fallback for GetTotalStaked should note the denominator is GetTotalMinted (not bank supply), so the corrected ratio uses bonded_tokens/total_minted; verify those units agree. Alternative smaller-still fix consistent with the nil-safe design: return NeutralBPS when the custom registry is empty (treat 'subsystem not yet in use' as 'no data'), deferring the SDK fallback question to the founder — that is a VALUES QUESTION overlay (is the sensor meant to measure zerone-native staking adoption, or actual consensus security?), but as shipped the docs' 'the chain announces what it sees' (Commitment 4, cited in sensors.go:100-105) is contradicted: it announces crisis about a subsystem while blind to the 82%-bonded reality securing it.

---

## [DESIGN_FLAW] Critical pacing is a permanent hidden 2x fee/cooldown constant that fires backwards: it throttles claims on an empty chain that needs claims, and no time/recovery path exists
**Area:** alignment-health

**Evidence:** GetGlobalPacingMultiplier returns 500000/2000000 for critical (x/alignment/keeper/pacing.go:24-25); x/knowledge/keeper/pacing.go:30-34 and 64-72 invert that into 2x cooldown and 2x min review fee. Live: /zerone/alignment/v1/global-pacing = critical/500000/2000000 on BOTH mainnet and testnet; composite is byte-identical 172527 across 782 mainnet and 1069 testnet observations — no sensor has any time term. Arithmetic of escape: KnowledgeQuality is already at its practical ceiling (800000: rate=1000000 from the sole accepted claim, diversity pinned neutral); accepted claims cannot raise composite except via throughput (fire_activity.go:30-43: theoretical max ~24 rounds per 10000-block window -> gov dimension max ~349000 -> composite ~229800, barely 'degraded'), and pacing itself doubles the cost of submitting those claims. Domains: +20 needed (+1400 composite each, 7 exist). Healthy (>=700000) needs ~78 registered validators and ~100 domains — years away. ApplyCorrections comment (x/alignment/keeper/corrections.go:95-99) confirms the slim cut retired the corrective actuator and kept only observation, leaving pacing as the sole live actuator wired from alignment into behavior.

**Smallest fix:** Gate pacing on data sufficiency: in GetGlobalPacingMultiplier, return neutral (BPS, BPS) unless the chain has ever been categorized healthy or a minimum-participation floor is met (e.g. any registered validator AND any completed round in window) — crisis means deterioration, not never-having-started.

**Question for the founder:** Is the genesis-era 2x fee an intentional bootstrap spam shield, or should health be neutral until the sensors have real participants to measure? Yu must pick: pacing-as-shield (document it honestly as a launch fee) or pacing-as-crisis-response (fix the gate).

**Skeptic's corrections:** CORE CONFIRMED: x/alignment/keeper/pacing.go:24-25 returns (500000, 2000000) for critical, keyed only on state.PreviousCategory — no time/recovery term anywhere (sensors.go:34-161 are all instantaneous ratios). x/knowledge/keeper/pacing.go:30-34 inverts creation pacing into 2x cooldown (50→100 blocks) and :64-72 into 2x min review fee (100000→200000 uzrn), enforced at msg_server.go:63-67 and :131-138. Live mainnet (169.155.55.44): global-pacing = critical/500000/2000000; state = previousCategory critical, 785 observations. corrections.go:95-99 comment confirms the autopoiesis actuator was retired (slim cut) and corrections record with applied=false; the only alignment→behavior actuators left are the pacing multipliers (knowledge creation throttle + capture_defense/keeper/keeper.go:116-118 analysis speedup). Critical is genesis-native: 3 of 5 dimensions are hard 0 (economicStability, networkSecurity, stakingRatio — no registered validators in custom staking; target hardcoded 111 at x/staking/keeper/alignment_adapters.go:41-43). EVIDENCE ERRORS: (1) Composite is NOT byte-identical 172527 across 782 mainnet observations. Live: scores/100 and scores/40000 = 109800; scores/77000 and scores/78500 = 172527. The first accepted claim (~h76944) DID move the composite +62727: verification rate neutral(500000)→1000000 lifted KQ 500000→800000 (+60000 composite) and 1-round throughput lifted gov 49000→62636 (+2727). So 'accepted claims cannot raise composite' is wrong as stated — but it is a one-shot jump to the rate ceiling; the corrected point stands: further accepted claims add only throughput. (2) Theoretical max rounds per 10000-block window is 22, not ~24 (commitPhase 200 + reveal 200 + aggregation 50 = 450; 10000/450 = 22, fire_activity.go:31-38). Gov ceiling ~349000 and claims-only composite ceiling ~229800 (barely degraded, 1.33x pacing) are still correct since throughput caps at BPS. (3) MISSED WORSENING: 'diversity pinned neutral' is only true until the first fitness-epoch aggregation (fitnessEpochBlocks=10000; ProcessDiversity wired at phases.go:68 → AggregateDomainDiversity diversity.go:358-401). Unanimous rounds have Shannon entropy 0, and 4-verifier panels with the 77% threshold force unanimity on every accept — so at the next epoch boundary the only domain with rounds gets AvgEntropy=0, GetGlobalConsensusDiversity (diversity.go:506-539) drops 500000→0, KQ drops 800000→600000, and the claims-only ceiling falls to ~189800 — BELOW the 200000 critical threshold. Sustained claim activity alone likely cannot escape critical at all; the trap is tighter than claimed. (4) MISSED MITIGATION: bootstrap-fund sponsorship (msg.Sponsored path, msg_server.go:141-151; fees.go:84-121; live params bootstrapFundEnabled=true, feeCap 5000000 uzrn >> the doubled 200000 fee, 10 claims/address, 100/50000-block epoch) lets first-truth submitters have the doubled fee paid by the genesis-funded knowledge_bootstrap_fund. The 2x fee tax on newcomers is softened — but it drains the communal fund 2x faster, is capped at 10 claims per address, and does nothing for the 2x cooldown, so the throttling harm stands.

**Skeptic's notes:** Severity slightly REDUCED on the fee side (sponsorship exists and covers the doubled fee for a newcomer's first 10 claims) but INCREASED on permanence: the unanimity→zero-entropy→diversity-collapse dynamic means even the claimed 'barely degraded' escape via max throughput is probably unreachable (ceiling ~189800 < criticalThreshold 200000 once epoch diversity records land), making critical effectively a fixed point until dozens of validators register and ~dozens of domains exist — none of which pacing touches. This is a genuine DESIGN FLAW, not a bug: every line works as documented ('Critical health doubles these effects', pacing.go:12), but the doc comment at knowledge/pacing.go:17-19 says degraded pacing 'matches reduced network throughput' — on this chain the causality is inverted: pacing reduces the throughput that the sensors need to see. On the fix: the proposed data-sufficiency gate is the right shape and fits naturally, since GetGlobalPacingMultiplier already reads alignment state — a has-ever-been-healthy (or min-participation) latch in AlignmentState is a one-field change. Two refinements: (a) gate degraded too, not just critical — 1.33x also fires from genesis; (b) the same multiplier feeds capture_defense's risk-analysis interval (analysis runs 2x FASTER under critical, which is beneficial), so either gate only the creation multiplier or accept losing the faster analysis in the primordial state. Alternative smaller fix consistent with the sensors' neutral-until-data philosophy (adapters return NeutralBPS when no data): have zero-data dimensions (no registered validators, zero supply staked) return NeutralBPS instead of 0, which lifts genesis composite to (800000+62636+3x500000)/5 ≈ 472527 = degraded→ nearly healthy and lets categorization mean what it says. Values question embedded: should never-having-started ever be called 'critical'? That word choice drives the throttle.

---

## [BUG] A chain born critical never emits network_health_critical and never activates 2x observation frequency — the penalty applies silently, the announcement never fires
**Area:** alignment-health

**Evidence:** module.go:190-224: the transition switch runs only when previousCategory != "" && previousCategory != category. Genesis state has PreviousCategory="" (x/alignment/types/genesis.go:87-95), the first observation categorizes critical and sets PreviousCategory=critical (module.go:225) without entering the switch; every subsequent observation is critical==critical, so the switch never fires. Verified live: both networks show previousCategory="critical" with degradedFrequencyActive=false after 782/1069 observations. docs/EVENTS.md:86 promises 'Network health composite dropped to critical level; defensive pacing activated' — that event has never been emitted on either network, yet pacing (which reads PreviousCategory directly, pacing.go:19) has been punishing users the whole time.

**Smallest fix:** Treat first categorization as a transition: change the guard to fire the critical/degraded cases when previousCategory == "" as well (or set DegradedFrequencyActive = category != healthy unconditionally each observation).

**Skeptic's notes:** Independently re-derived; claim is a confirmed BUG. Guard at x/alignment/module.go:190-191 (previousCategory != "" && != category) plus genesis PreviousCategory="" (types/genesis.go:87-95, stored verbatim by keeper/state.go:314) means a born-critical chain never enters the transition switch. Live zerone-1: previousCategory="critical", degradedFrequencyActive=false, observationCount=785 at height 78500 — 785x100=78500 proves the halved interval (module.go:147-149) never engaged. Event-emission null verified with control: block_search network_health_critical = 0 blocks vs observation_recorded = 785 blocks, so it is not an indexing artifact. Two precision notes: (a) the fee doubling flows through the CREATION multiplier (pacing.go:25 returns 500_000; knowledge/keeper/pacing.go:65-72 computes base*1_000_000/500_000 = 200000uzrn, matching tonight's fee), not the analysis multiplier — the claim's mechanics text is compatible but should cite creationBps; (b) mainnet observation count is now 785, not 782. Severity amplifier found: the switch also misses critical->degraded improvement (degraded case at module.go:193 requires previous==healthy), so a born-critical chain that partially recovers still emits nothing and DegradedFrequencyActive stays false — the proposed unconditional-set fix (DegradedFrequencyActive = category != healthy each observation) closes both gaps, while the narrower guard-relaxation fix closes only the first-categorization gap. Either fix mutates EndBlock state/events and is consensus-breaking: ship via coordinated upgrade handler, not a silent binary swap.

---

## [BUG] Epoch diversity aggregation is off-by-one: it always processes the epoch that just STARTED (empty), so no DomainDiversityRecord is ever written — the diversity sensor and conformity alerts are dead code
**Area:** alignment-health

**Evidence:** x/knowledge/keeper/phases.go:41-42: at boundary height%FitnessEpochBlocks==0, epoch := height/FitnessEpochBlocks — the NEW epoch's number. But rounds are indexed under the epoch of their completion height (diversity.go:303-306), so AggregateDomainDiversity(domain, epoch) (diversity.go:358-401, sole call site phases.go:68) iterates an epoch that contains at most same-block completions and returns nil at roundCount==0 (diversity.go:386-388); the just-ended epoch's rounds are never aggregated. Consequence: GetGlobalConsensusDiversity (diversity.go:506-539) finds no record and returns NeutralBPS forever. Verified live: testnet at height 106900 (>10 epoch boundaries after its drilled rounds) still shows knowledgeQuality=800000 = 0.6*1000000 + 0.4*500000, i.e. diversity pinned at neutral.

**Smallest fix:** In phases.go:42, aggregate the epoch that just ended: pass epoch-1 to ProcessDiversity (guarding epoch>0).

**Skeptic's corrections:** Core off-by-one CONFIRMED from code: rounds are indexed under epoch = completionHeight/FitnessEpochBlocks (diversity.go:303-305,322), and at boundary H=N*FEB phases.go:41-42 computes epoch=N (the epoch that just STARTED) and calls ProcessDiversity(N) (phases.go:68, sole call site); ProcessDiversity(N-1) ran one epoch earlier before its rounds existed, so ended epochs are never aggregated. SetDomainDiversity's only writer is AggregateDomainDiversity (diversity.go:400), which returns nil at roundCount==0 (diversity.go:386-388); CheckConformityAlert is only reachable when a record is found (diversity.go:490-494); CompleteRound is only reachable from BeginBlocker (rounds.go:58, phases.go:229). Two corrections: (1) 'no DomainDiversityRecord is EVER written' is slightly overstated — AdvanceRoundPhases (phases.go:21) runs before ProcessDiversity (phases.go:68) in the same BeginBlocker, so a round whose reveal/aggregation deadline lands exactly on a multiple of FitnessEpochBlocks completes with epoch=N and IS captured by ProcessDiversity(N) that block; measure-zero in practice, but not impossible. (2) The cited live evidence is misdescribed: zerone-testnet-1 (FEB=10000, height 107151) has its only completed round at height 103925 = epoch 10, so ZERO epoch boundaries have elapsed since its drilled rounds (next is 110000), not '>10' — the 10 boundaries since genesis had nothing to aggregate anyway (the 47 genesis doctrine facts never went through verification rounds, so no RoundDiversityRecords existed). The observed knowledgeQuality=800000 (verified live: testnet obs @107100 and mainnet obs @78500 both 800000; DomainDiversityHistory empty for all 7 testnet domains) is consistent with the bug but NOT yet discriminating — correct code would also show 800000 today. Discriminating live test: after testnet height 110000, buggy code stays 800000; fixed code would show 600000 during heights 110000-119999 (unanimous 4/4 round → entropy 0 → diversity 0). Mainnet's first fact (round completed 76944, epoch 7, FEB=10000) will likewise be silently dropped at boundary 80000 under current code.

**Skeptic's notes:** Verdict: BUG, correctly classified — sensors.go:31 ('A system that verifies everything unanimously scores LOWER on knowledge quality') is inert because senseKnowledgeQuality's diversity input (sensors.go:104 via alignment_adapters.go:95 → GetGlobalConsensusDiversity, diversity.go:506-539) can essentially never find a record, and R28-2 conformity alerts share the same dead input. Proposed fix (pass epoch-1 in phases.go:42-68) is correct and minimal; the epoch>0 guard is unnecessary since the branch requires height>0 and height%FEB==0, making epoch>=1, though it is harmless. Two follow-ons the fix surfaces: (a) with the fix, the chain's structurally forced 4/4 unanimity immediately drives diversity to 0 and knowledgeQuality to 600000, and conformity streaks (threshold 50000 BPS, 3 epochs — live params confirmed) will start counting in round-bearing epochs — this is the doc-promised behavior, but the founder should expect the health index to DROP after deploying the fix; (b) GetGlobalConsensusDiversity's 2-epoch lookback (diversity.go:517, try<2) means diversity reverts to NeutralBPS after two round-less epochs even with the fix, so on a low-traffic chain the sensor is neutral most of the time — a separate DESIGN FLAW worth flagging (smallest honest fix: persist last-known epoch with data, or widen lookback). Whether ProcessCompetition/ProcessMetabolism share the same new-epoch-number convention was not audited here; if they interpret epoch as 'the ended epoch' elsewhere, fixing only diversity keeps the codebase's epoch semantics inconsistent — worth a quick consistency pass when applying the fix.

---

## [DESIGN_FLAW] KnowledgeQuality rewards accept-RATE, so honestly rejecting a false claim deepens 'critical' and raises everyone's fees — while the purpose-built ungameable sensor sits unused
**Area:** alignment-health

**Evidence:** sensors.go:38 uses knowledgeKeeper.GetVerificationRate = accepted/terminal (x/knowledge/keeper/alignment_adapters.go:25-47; REJECTED, MALFORMED and INSUFFICIENT all count against). Live: 1 accepted claim -> rate 1000000 -> KQ 800000. One honest rejection would make rate 500000 -> KQ 500000 -> composite 112527, 60000 deeper into critical. The same file defines GetSurvivedChallengeRate (alignment_adapters.go:49-79) whose own comment says accept-rate 'can be gamed by rubber-stamping' and that 'rejecting a false claim... raises the rate rather than lowering it' — but the alignment sensor does not use it.

**Smallest fix:** Swap sensors.go:38 to GetSurvivedChallengeRate (neutral until challenges resolve, per its contract), or blend it in at majority weight.

**Skeptic's corrections:** Core mechanics CONFIRMED from code + live queries, with one overbroad phrase and one framing correction. Confirmed: x/alignment/keeper/sensors.go:38 feeds KnowledgeQuality with knowledgeKeeper.GetVerificationRate (60% weight, 40% diversity, lines 46-47); x/knowledge/keeper/alignment_adapters.go:25-47 computes accepted/terminal where REJECTED, MALFORMED and INSUFFICIENT all increment terminal without accepted (INSUFFICIENT is genuinely reachable — set at x/knowledge/keeper/phases.go:189 and rounds.go:119, so it is not dead code). GetSurvivedChallengeRate exists at alignment_adapters.go:49-79 with exactly the quoted anti-rubber-stamping contract, and the alignment sensor cannot even call it: alignmenttypes.KnowledgeKeeper (x/alignment/types/expected_keepers.go:9-20) does not include the method. Live zerone-1 confirmed every number: observation @78500 has knowledgeQuality=800000 (rate 1000000 x 0.6 + neutral diversity 500000 x 0.4), composite=172527, category critical (params: all five weights 200000, criticalThreshold 200000); natural experiment — KQ jumped 400000 to 800000 across the first acceptance at 76944. Counterfactual arithmetic exact: one honest rejection makes rate 1/2=500000, KQ=500000, composite=112527, i.e. 60000 deeper into critical (weight 200000 x 0.6 x 500000 drop). Fee doubling path confirmed: critical category returns creationBps=500000 (x/alignment/keeper/pacing.go:24-25); GetEffectiveMinReviewFee scales inversely (x/knowledge/keeper/pacing.go:53-76) giving 100000 x 2 = 200000uzrn = 0.2 ZRN; live /zerone/alignment/v1/global-pacing shows critical/500000/2000000. CORRECTION 1: "the purpose-built ungameable sensor sits unused" is overbroad — GetSurvivedChallengeRate IS live-wired into block-reward issuance via the survival-gate coupling (x/vesting_rewards/keeper/rewards.go:402-425, via x/knowledge/keeper/vesting_rewards_adapters.go:32-33). It is unused only by the alignment health sensor, the subject of this finding. CORRECTION 2: the chain is ALREADY critical (172527 < 200000) and fees are already doubled; a rejection today does not newly raise fees — it pushes the composite further below the exit threshold, prolonging the doubled-fee state and making recovery harder. The fee-RAISING effect of a rejection is marginal only near category boundaries (degraded->critical doubles the fee; healthy->degraded takes it to ~1.33x). Direction and magnitude of the perverse incentive are exactly as claimed.

**Skeptic's notes:** Three things that change severity or the fix. (1) Severity UP: the accept-rate has unbounded lookback — GetVerificationRate iterates ALL claims ever (alignment_adapters.go:27), so every honest rejection permanently depresses network health with no windowing or decay; and the supposed counterbalance, the 40% diversity term (sensors.go:31 'verifies everything unanimously scores LOWER'), does not rescue it: it rewards disagreement, not correctness — a unanimous honest rejection scores entropy 0 identical to unanimous rubber-stamping (diversity.go:293, ComputeRoundEntropy on accept/reject counts) — and it is currently inert anyway because DomainDiversityRecord is only written by epoch aggregation (diversity.go:358-401) and GetGlobalConsensusDiversity (diversity.go:506-539) returns neutral 500000 until then. (2) Fix is NOT a one-line swap: GetSurvivedChallengeRate is absent from alignmenttypes.KnowledgeKeeper (x/alignment/types/expected_keepers.go), so the interface needs the method added; the adapter already implements it (compile-time assert at alignment_adapters.go:22 passes), so it is a ~3-line change, but touches the module boundary. (3) Fix caveat: a naked swap at current chain state would DROP KQ from 800000 to 500000 (survived-challenge rate is neutral 500000 until the first challenge resolves — challengeDurationBlocks=34272, none resolved yet), moving composite to 112527, i.e. the fix itself deepens critical today. The blend variant (or rate = max(survivedRate, neutral-blended accept signal)) is the smallest honest fix that removes the anti-rejection incentive without punishing the current healthy behavior. VALUES-QUESTION flavor for the founder: whether alignment health should react at all to accept-rate before any adversarial challenge has ever resolved, or sit at neutral until the survival signal has data.

---

## [BUG] Staked/supply is double-counted (40% of composite) and its denominator is a wrong proxy (18.35 ZRN of 'minted' vs 13,573 ZRN actual supply), creating a saturation cliff
**Area:** alignment-health

**Evidence:** senseStakingRatio (sensors.go:151-161) is byte-for-byte the same computation as senseEconomicStability (sensors.go:58-67) — same two keepers, same ratio — despite the comment claiming it is 'measured independently'; with equal weights (params: five weights of 200000) one signal owns 40% of the composite. Denominator: GetTotalSupply 'uses GetTotalMinted as a proxy' because bankKeeper is private (x/vesting_rewards/keeper/alignment_adapters.go:26-38). Live: /zerone/vesting_rewards/v1/supply_coupling totalMinted=18351026 uzrn (18.35 ZRN) vs currentSupply=13573351026 (13,573 ZRN). ratioBPS caps at BPS (sensors.go:185-201), so the first participant to register and stake >=19 ZRN in the custom module flips BOTH dimensions from 0 to 1000000 simultaneously (+400000 composite in one step).

**Smallest fix:** Expose bankKeeper.GetSupply("uzrn") through the vesting_rewards adapter for the denominator, and replace one duplicate dimension with a genuinely independent signal (e.g. survived-challenge rate) or halve the two weights.

**Skeptic's corrections:** All core mechanics re-derived and confirmed from code + live mainnet (height ~78534).

DUPLICATION (BUG): senseStakingRatio (sensors.go:154-161) is byte-for-byte identical to senseEconomicStability (sensors.go:60-67) — both do ratioBPS(stakingKeeper.GetTotalStaked, vestingRewardsKeeper.GetTotalSupply). Comment at sensors.go:152 claims "measured independently" — code contradicts intent. Both are read in ObserveAll (sensors.go:22,25) and both weighted in ComputeScores (scoring.go:14-18). Live params: all 5 weights=200000 → each dim=20%, duplicate pair=40%.

DENOMINATOR (BUG): AlignmentVestingRewardsAdapter.GetTotalSupply (alignment_adapters.go:27-38) returns GetTotalMinted "because bankKeeper is private." Live totalMinted=18351026 uzrn (18.35 ZRN) vs bank supply 13573351026 uzrn (13,573 ZRN) — ~740x understatement (genesis allocation never counted as minted). The "private" excuse is FALSE: alignment_adapters.go is `package keeper`, same package as the bankKeeper field (keeper.go:24), and the keeper already calls k.bankKeeper.GetSupply(ctx,"uzrn") at keeper.go:307. So a.k.bankKeeper.GetSupply(...) is a legal one-line fix — easier than the finding implies.

CLIFF: ratioBPS caps at BPS (sensors.go:197-200). Custom staking module currently empty (live validators:[], GetTotalBondedStake=0 per keeper.go:143), matching live obs economicStability=0, stakingRatio=0 at height 78500. Min self-delegation to register = 111000 uzrn = 0.111 ZRN (params), IsActive=true set at registration for any tier (msg_server.go:94). To SATURATE both dims to 1000000 needs totalStaked >= totalMinted ~= 18.35 ZRN; below that both move proportionally in lockstep (still double-counted). Saturating adds (1000000*200000*2)/BPS = +400000 composite.

**Skeptic's notes:** Two evidence overstatements that do NOT change severity: (1) After the flip composite goes 172527 -> ~572527, which is category "degraded" (degradedThreshold 400000 <= x < healthyThreshold 700000, scoring.go:34-40), NOT healthy — the finding's "degraded/healthy" reach overstates by one band. (2) The "~19 ZRN flips 0->1000000" is exact only at staked>=totalMinted; smaller stakes give partial values. The double-counting holds at every stake level.

Severity note that STRENGTHENS the finding: the proposed fix's premise ("bankKeeper is private") is provably wrong — same-package access is legal and already used at keeper.go:307 — so the "proxy" was an unnecessary shortcut, making this a clean BUG rather than a constrained tradeoff. Recommend: (a) denominator fix = replace GetTotalMinted with a.k.bankKeeper.GetSupply(ctx,"uzrn").Amount.BigInt() in alignment_adapters.go:27-38; (b) de-duplicate — either drop one dimension's weight to 0 or replace senseStakingRatio with a genuinely independent signal (survived-challenge/dispute-survival rate), since the comment already promises independence. Both are BUGs (code contradicts stated intent); the saturation-cliff consequence is a DESIGN FLAW downstream of the denominator BUG.

---

## [DESIGN_FLAW] Advisory-correction noise grows unbounded and degraded_threshold is not used for categorization
**Area:** alignment-health

**Evidence:** GenerateCorrections emits 3 records per observation while stake dims sit below DegradedThreshold (corrections.go:39-90); all are applied=false by design since the slim cut retired the actuator (corrections.go:95-99). Live mainnet: /zerone/alignment/v1/corrections total=2349 and growing every 100 blocks; PruneOldOutcomes (correction_confidence.go:134-143) prunes outcomes, not correction records. Separately, CategorizeHealth (x/alignment/keeper/scoring.go:32-41) uses only CriticalThreshold and HealthyThreshold; params.DegradedThreshold=400000 (documented at docs/PARAMETERS.md:449 as 'Degraded alignment threshold') affects only correction generation, not the category boundary — degraded actually spans 200000..699999.

**Smallest fix:** Skip storing a correction identical to the previous observation's (or prune correction records older than N observations), and either wire DegradedThreshold into CategorizeHealth or correct PARAMETERS.md to say it only scales advisory correction magnitude.

**Skeptic's corrections:** Core mechanics confirmed, two refinements: (1) Rate is ~16x WORSE than claimed. Live zerone-1 at height 78533: /zerone/alignment/v1/corrections total=2355 = exactly 3 x (78533/100) observations, i.e. 3 records every 100 blocks since genesis (economic_stability, network_security, staking_ratio; knowledge_quality stays above threshold). Measured block time is ~2.53s (blocks 78400->78500 span 253s), so the run rate is ~374,000 records/year, not 23k+ — the claimed harm understates reality. Latest records (height 78500) are byte-identical to the earliest (height 100) except height/timestamp: same 3 dims, magnitude 800000 each ((400000-0)x2, scores are 0 and below CriticalThreshold so doubled per x/alignment/keeper/corrections.go:41-42). (2) Generation spans corrections.go:22-90 (4 dims can fire, knowledge_quality at :22-36), not just :39-90. Everything else verified as stated: no code path ever sets Applied=true (GenerateCorrections hardcodes false at corrections.go:33,50,70,87; ApplyCorrections re-asserts false at :143,:170,:196 and the fall-through at :201-207 just logs 'no regulator wired' and stores); AddCorrection (x/alignment/keeper/state.go:175-191) only appends, no delete path for CorrectionKeyPrefix exists anywhere; PruneOldOutcomes (correction_confidence.go:135-180) iterates only CorrectionOutcomeKeyPrefix; CategorizeHealth (x/alignment/keeper/scoring.go:32-41) uses only CriticalThreshold(200000) and HealthyThreshold(700000), so 'degraded' spans 200000..699999; repo-wide grep shows DegradedThreshold's only consumers are GenerateCorrections and the ordering validation at x/alignment/types/genesis.go:122,126; docs/PARAMETERS.md:449 documents it as 'Degraded alignment threshold', which does not match its actual role (correction trigger + magnitude baseline).

**Skeptic's notes:** Severity is higher than claimed: ~374k records/year at measured 2.53s blocks (roughly 60-80MB/year of JSON state), and ExportGenesis exports ALL correction records unbounded (state.go:374, GetCorrections with limit=0), so genesis exports/upgrades inherit the bloat. The proposed dedup fix would be maximally effective here since live records are identical modulo height/timestamp. Two adjacent findings that sharpen the fix decision: (a) ApplyCorrections records a CorrectionOutcome for every generated correction even though nothing is ever applied (corrections.go:112-121), and EvaluatePendingCorrections then scores 'success' by whether the dimension drifted toward HealthyThreshold on its own (correction_confidence.go:100-104) — so correction 'confidence' measures random drift, not efficacy, and it feeds GetEffectiveObservationInterval (correction_confidence.go:68-81): if drift-confidence drops below 300000 the interval shrinks to 66 blocks and the noise rate rises another ~50%. (b) Cosmetic: AddCorrection truncates the uint64 counter to uint32 for the store key index (state.go:180). Categorization: DESIGN_FLAW is the right label — code matches its own comments (the slim-cut advisory intent at corrections.go:95-99) but contradicts PARAMETERS.md, so the DegradedThreshold half is a small doc-honesty BUG fixable by one line in docs/PARAMETERS.md (e.g. 'threshold below which advisory corrections are generated'); wiring it into CategorizeHealth would instead change live health categories (current dims at score 0 already read critical/degraded correctly, but the boundary semantics would shift) and needs a founder call.

---

## [VALUES_QUESTION] Should genesis emptiness be 'critical' at all? (the founder's call the code currently makes silently)
**Area:** alignment-health

**Evidence:** Everything above compounds into one lived fact: both networks have spent their entire lives (782 and 1069 observations) in 'critical' with composite frozen at 172527, 2x fees applied, no event emitted, no path out except registering into a module with zero participants. EVENTS.md:86 frames pacing as 'defensive'; docs/FIRST-TRUTH.md:130 already had to warn ceremony participants about the doubled fee — the docs are adapting to the bug instead of the code keeping the docs' promise.

**Smallest fix:** NONE

**Question for the founder:** Yu: when the sensors have no participants to measure, should the chain say 'I don't know yet' (neutral, no pacing) or 'I am in crisis' (current behavior)? And if the bootstrap 2x fee stays, will you document it in PARAMETERS.md/FIRST-TRUTH.md as an intentional launch constant rather than an emergent accident?

**Skeptic's corrections:** Core claim CONFIRMED, two details corrected. CONFIRMED: (1) Both networks have been 'critical' every observation of their lives — live queries: mainnet /zerone/alignment/v1/state shows previousCategory=critical, observationCount=785 (was 782); testnet 37.16.28.121 shows 1071 (was 1069); health index sampled at mainnet heights 100/10000/30000/50000/70000/76900/78500 is 'critical' at every point. (2) 2x fee is real: critical → creationPacing=500000 (x/alignment/keeper/pacing.go:24-26), and GetEffectiveMinReviewFee = base*1000000/500000 = 2x (x/knowledge/keeper/pacing.go:66-74); claim cooldown also doubles (pacing.go:28-35); lived fee 200000uzrn matches. (3) The documented alarm event network_health_critical (docs/EVENTS.md:86, 'defensive pacing activated') has NEVER fired on either network: module.go:190-191 emits only when previousCategory != "" && previousCategory != category, and DefaultGenesis leaves PreviousCategory unset (x/alignment/types/genesis.go) — a chain born critical never 'drops to' critical, then previous==current forever. (4) The 3 dead dimensions (economicStability, networkSecurity, stakingRatio all 0) trace to zerone's OWN x/staking module having zero participants — live /zerone/staking/v1/validators returns total:0 on both networks; sensors are wired to it via AlignmentStakingAdapter (app/app.go:983, x/staking/keeper/alignment_adapters.go:27-43; target hardcoded 111). (5) docs/FIRST-TRUTH.md:129-130 does warn '0.2 ZRN, not 0.1' — docs adapting to the behavior, confirmed. CORRECTED: (a) Composite is NOT 'frozen at 172527' for the chains' entire lives — mainnet was 109800 from h100 through h70000+, dipped to 89800 (~h76900, knowledgeQuality 400000), and rose to 172527 only after tonight's first fact acceptance (h77000+, knowledgeQuality 800000, governance 62636; 0.2*(800000+62636)=172527). It responds to knowledge activity; both networks read 172527 now because they share identical knowledge/ontology state. (b) 'No event emitted' is overstated: zerone.alignment.observation_recorded fires every 100 blocks carrying category=critical and both multipliers (verified live in block_results at h78500). What is missing is only the dedicated transition/alarm event. (c) 'No path out except registering into a module with zero participants' is slightly overstated: exiting critical (composite >= 200000, strict < in scoring.go:34) is arithmetically possible via knowledge+ontology alone — needs knowledgeQuality+governance dims to sum >= 1,000,000; currently 862,636, so ~20 more registered domains (+1400 composite each: domainScore=count*BPS/100, 70% weight, 20% dim weight) or near-perfect knowledgeQuality would cross into degraded. However HEALTHY (>=700000) is impossible without staking: non-staking max composite = 0.2*(1M+1M) = 400,000, so a fee surcharge (1.33x in degraded) persists until x/staking bootstraps.

**Skeptic's notes:** Severity slightly REDUCED from the claim's framing (state is publicly announced every 100 blocks via observation_recorded, so 'silent' means 'no alarm event and no doc for the constant', not 'invisible'), but the VALUES_QUESTION stands and gains one aggravating fact: the alignment corrections loop is also spinning uselessly — every observation generates 3 governance-required corrections (increase staking.reward_rate / slashing_severity) whose outcomes record successful=false with score_before=0 score_after=0 forever (live events at h78500), i.e., the chain asks for help every 4 minutes into a governance void. Also note CategorizeHealth never uses the DegradedThreshold param (400000) — the degraded band is [critical, healthy) per scoring.go:36-38 — a minor code/params honesty gap worth flagging alongside. Smallest honest fixes for the founder to choose among: (1) treat previousCategory=="" as a transition in module.go:191 so a chain born critical emits network_health_critical at first observation (makes docs/EVENTS.md:86 true, one-line change); (2) genesis grace: return neutral pacing (BPS,BPS) while the staking module has zero registered validators or for the first N observations — encodes 'infant != failing' (COMPASSION doctrine) directly; (3) if the 2x-in-critical fee is intended, promote it from emergent behavior to a documented protocol constant in TRUTH_SEEKING/FIRST-TRUTH and EVENTS.md. Option 2 is the values call; options 1 and 3 are honesty repairs that are safe regardless.

---

## [DESIGN_FLAW] Forced unanimity at minimum panel size punishes honest dissent and rewards coordination
**Area:** economics

**Evidence:** fire_activity.go:57-72 sets effectiveMin = MinVerifiers+1 = 4 for domain claims ('No social structure -> Fire burns unchecked'); live params (http://169.155.55.44:1317/zerone/knowledge/v1/params): minVerifiers=3, confidenceThreshold=770000, minHeadcountAgreement=3. At stake-1 weights, 3/4 = 750000 < 770000 (confidence.go:133-146) so only 4/4 lands a verdict — confirmed by tonight's fact ecb5004a requiring 4/4 unanimity. One dissenter → INCONCLUSIVE: submitter's non-refundable fee already distributed at submission (msg_server.go:163-166, rounds.go:118 'Review fee is non-refundable'), submitter records Inconclusive++ (rounds.go:84 → agent_calibration.go:153-155), claim dies INSUFFICIENT (rounds.go:117-121). Verifiers get no rewards AND no slashes on INCONCLUSIVE (confidence.go:192-194), the 55% verifier pool is stranded in the module account (distributeVerifierRewardsFromPool returns early, rounds.go:293), and UpdateValidatorIndependence never runs because verdictToVoteString(INCONCLUSIVE)="" (rounds.go:248-255, diversity.go:542-552) — the dissenting minority vote is structurally unrecordable at min panel size, the exact opposite of what applyIndependenceMultiplier (rounds.go:333-355) exists to reward.

**Smallest fix:** Governance param change: ConfidenceThreshold 770000 → 750000, so 3-of-4 (still a supermajority, still gated by minHeadcountAgreement=3) lands a verdict and one honest dissenter is tolerated. Complementary one-liner: run UpdateValidatorIndependence on INCONCLUSIVE rounds using the plurality vote so dissent is at least recorded. A 3-of-5 design (effectiveMin=5, threshold 600000) tolerates two dissenters but costs more panel liquidity.

**Question for the founder:** Is unanimity-at-quorum deliberate epistemic conservatism ('only facts no seated witness doubts') or an accident of 770000 vs 750000 arithmetic? If deliberate, are you willing to accept that it makes conformity the payment condition and free griefing the dissent condition?

**Skeptic's corrections:** Core claim CONFIRMED by independent re-derivation, but three mechanics are subtly different. (1) "Verifiers get no rewards AND no slashes on INCONCLUSIVE" is only true for stake: calculateRewardsAndSlashes does return early (confidence.go:192-194), but CompleteRound still feeds capture defense on INCONCLUSIVE — rounds.go:222-239 computes verdictVote=verdictToVoteString(INCONCLUSIVE)="" (diversity.go:542-552) and then calls UpdateReputation with wasCorrect=(reveal.Vote=="")=false for EVERY revealer, so all four verifiers (the three honest ones included) lose ~2% global capture-defense reputation per inconclusive round (x/capture_defense/keeper/reputation.go:33-44, wired live via app.go:967). This is an indiscriminate penalty, so it aggravates rather than mitigates the incentive inversion — but "costless" is inexact. (2) Submitter harm is overstated: Inconclusive++ is recorded (agent_calibration.go:154-155) but ComputeAgentCalibrationScore explicitly excludes Inconclusive from the denominator (agent_calibration.go:275-288, comment: "not evidence of (mis)calibration"), so the load-bearing calibrationScoreBps is unchanged; the submitter's real loss is the non-refundable fee only (msg_server.go:140-166, rounds.go:118). (3) The claim misses a strictly cheaper griefing vector: commit-then-withhold. With 3 reveals < effectiveMin=4, AggregateVerificationResult returns INCONCLUSIVE at confidence.go:96-99 BEFORE calculateRewardsAndSlashes is ever called, so missedRevealSlashBps=100000 (10%) is never applied on a min-size panel; and the withholder is absent from round.Reveals, so even the capture-defense reputation ding skips them while still hitting the three honest revealers. Everything else re-verified exactly as cited: effectiveMin=MinVerifiers+1=4 (fire_activity.go:57-72, +capture override at :68); live params minVerifiers=3, confidenceThreshold=770000, minHeadcountAgreement=3; stake-1 fallback for unregistered verifiers (confidence.go:53-55) — and the live fact's own disclosure text confirms the 4 panel keys have "no zerone.staking registrants", so mainnet panels really are stake-1; verdict switch at confidence.go:129-146 (headcountFloor=min(3,3)=3; 3/4=750000<770000 → only 4/4 lands); INCONCLUSIVE→INSUFFICIENT at rounds.go:117-121; pool early-return at rounds.go:292-295 with the 55% left in the knowledge module account (fees.go:38-43 — nuance: it is commingled/fungible there, not earmarked, so it silently pads future payouts rather than being provably locked); independence update gated on majorityVote!="" at rounds.go:248-255; applyIndependenceMultiplier at rounds.go:333-355. Live fact ecb5004ae763034f6dacb27832c34191 (claim 070fbd0b2fb26bf92933e3302be00947, verifiedAtBlock 76944, confidence 880000) confirmed via GET /zerone/knowledge/v1/facts/{id}. One scoping check: the pathology is specific to the 4-seat minimum — at 5 seats 4/5=800000≥770000 tolerates one dissenter — so "at exactly the panel size scarcity makes typical" is correct.

**Skeptic's notes:** Severity slightly HIGHER than claimed, fix needs two amendments. (a) The proposed ConfidenceThreshold 770000→750000 works as stated (governance param; 3-of-4=750000 passes with headcount 3≥floor 3), but "one honest dissenter is tolerated" is imprecise: once 3-of-4 lands a verdict, the dissenter is slashed wrongVerificationSlashBps=5% as a wrong voter (vindication-eligible escrow on ACCEPT, rounds.go:150-178) — tolerated by the round, punished personally. Currently inert on mainnet (no staking registrants, per the fact's own disclosure), but it becomes real the moment verifiers stake. (b) The fix does NOT close the cheaper griefing vector: a withholding verifier still kills the round via the reveals<effectiveMin quorum check (confidence.go:96-99) with zero slash — the missed-reveal slash is unreachable on min-size panels because the early return skips calculateRewardsAndSlashes. Smallest complementary fix: compute missed-reveal slashes even on quorum-fail/INCONCLUSIVE rounds (move the committed-but-not-revealed loop ahead of the early returns). (c) New adjacent bug worth its own finding: on INCONCLUSIVE, rounds.go:222-239 marks ALL revealers wasCorrect=false in capture-defense reputation (verdictVote=""), decaying honest verifiers' reputations ~2% per inconclusive round — INCONCLUSIVE should skip reputation updates exactly like it skips independence updates (rounds.go:249 has the "" guard; the capture-defense block at 221 lacks it). (d) Minor inconsistency: phases.go:170 gates late aggregation on params.MinVerifiers (3), not effectiveMin (4) — a round with 3 reveals still enters CompleteRound as INCONCLUSIVE rather than expiring. (e) The submitter-reputation harm should be dropped from the finding (calibration-score-neutral by design); the submitter harm is the fee alone, which at minReviewFee=0.1 ZRN (0.2 under critical pacing) makes dissent-griefing cheap to inflict but also cheap to absorb — the stronger argument is the conformity-as-payment-condition incentive, which fully stands: rewards require a decisive verdict + voting with it, independence credit is structurally unrecordable on 4-seat inconclusives, and capture-defense reputation punishes honest majorities for a griefer's dissent.

---

## [DESIGN_FLAW] The 100-ZRN wall is hardcoded, exclusionary at current supply, and simultaneously flash-shareable by sybils
**Area:** economics

**Evidence:** msg_server.go:288-299: 'Verifier minimum balance gate (stopgap until full qualification module)'; minBalance := sdkmath.NewInt(100_000_000) hardcoded, not in Params. Live supply query: 13,573,351,026 uzrn total — 100 ZRN = 0.74% of all ZRN per verifier seat; a 4-seat panel needs ~3% of total supply spendable, essentially all ops-held. Bootstrapping via verifying is impossible: at ~27,500 uzrn per correct vote (see witness-pay finding) an outsider needs ~3,636 correct rounds to earn one seat. Yet the gate checks spendable balance only at commit time — nothing bonds or re-checks it at reveal (msg_server.go:350-380) — so one 100-ZRN bag can be passed between sybil accounts across blocks within the same 200-block commit window to seat multiple 'independent' verifiers. Note the full qualification module the comment defers to already exists and is wired (x/qualification/keeper/keeper.go:129-135, checked at msg_server.go:302-327).

**Smallest fix:** Move the threshold into Params (proto-first per project rules) so governance can scale it with supply, and either escrow it for the round's duration or re-check at reveal to kill flash-sharing. Longer path: delete the balance gate and let x/qualification (already wired at msg_server.go:302) be the whole gate, as the comment always promised.

**Question for the founder:** Should witnessing be capital-gated at all, or is qualification (track record in x/qualification) the real Sybil defense — and if capital-gated, what fraction of live supply is an acceptable price for one seat at the truth table?

**Skeptic's corrections:** Core claim re-derived and CONFIRMED, with three refinements. (1) Hardcoding: x/knowledge/keeper/msg_server.go:288-299 has minBalance := sdkmath.NewInt(100_000_000) with the literal comment 'Verifier minimum balance gate (stopgap until full qualification module)'; no min_balance field exists in proto/zerone/knowledge/v1/genesis.proto Params, and the live params query (/zerone/knowledge/v1/params) shows no such param. (2) Supply math: live query /cosmos/bank/v1beta1/supply/by_denom?denom=uzrn returns 13,573,351,026 uzrn, so 100 ZRN = 0.737%/seat, ~2.95% for a 4-seat panel — as claimed. (3) Flash-sharing: confirmed but the wording 'spendable balance' is slightly wrong — the gate uses bankKeeper.GetBalance (msg_server.go:294, expected_keepers.go:24), i.e. raw bank balance, not SpendableCoins; vesting-locked coins pass the gate (though only liquid coins can be flash-transferred). Check runs only in SubmitCommitment; SubmitReveal (msg_server.go:350-395) and StoreRevealInRound (state.go:463-494) never touch balances; nothing escrows; commit is fully open/self-selecting (state.go:444-454 appends any committer to SelectedVerifiers); live commitPhaseBlocks=200 confirms the window. The alternate vote-extension commit path (app/abci.go:449-473) is VRF-selection-gated to bonded validators, so it doesn't rescue the open tx path. (4) Bootstrap math: fees.go:18 reviewFeeContributorBps=550_000; critical-pacing fee 200,000 uzrn → 110,000/4 = 27,500 uzrn per correct vote, matching the cited figure — but the claim UNDERSTATES the exclusion: since you cannot commit at all below 100 ZRN, earning the seat via verifying is circular (literally impossible, not merely ~3,636 rounds slow). (5) Qualification module: exists and wired exactly as cited (x/qualification/keeper/keeper.go:129-135, checked at msg_server.go:302-327).

**Skeptic's notes:** One material caveat on the proposed 'longer path' fix (delete the balance gate, let x/qualification be the whole gate): today that would remove the ONLY gate. Live query /zerone/qualification/v1/domain/general returns zero qualifications, and msg_server.go:312-325 contains a permissive fallback that admits unqualified verifiers with only a log warning whenever qualified count < effectiveMin — so with zero qualifications the qualification check is a no-op passthrough for every domain including the one the first mainnet fact landed in. Deleting the balance gate without first tightening that fallback leaves SubmitCommitment ungated. Also note x/qualification has its own bootstrap wall: live params show minStakeAmount=100,000,000 uzrn (the same 100 ZRN, but LOCKED for stakeLockPeriod=100,800 blocks — which is exactly the escrow semantics the short fix asks for) plus minVerifications=100 and minAccuracyBps=800,000 — a chicken-and-egg that reproduces the exclusion problem in a different shape and needs its own bootstrap ramp. The short-path fix (move threshold to Params via proto-first, escrow or re-check at reveal) is therefore the right first move; severity of the finding is if anything slightly higher than claimed because balance-gate exclusion is circular (can't earn the seat by verifying at all) and because the sybil flash-share needs only ordinary bank sends inside a single 200-block window.

---

## [DESIGN_FLAW] No economic reason for a stranger to witness: verification pays ~8x less than bridge attestation for ~450x more capital
**Area:** economics

**Evidence:** Verifier pay = 55% of the review fee split equally among correct voters (fees.go:18-20, rounds.go:292-324): tonight's 200,000-uzrn fee → 110,000-uzrn pool / 4 = 27,500 uzrn (~0.0275 ZRN) per verifier, against 100 ZRN parked (msg_server.go:295) and slash exposure (wrongVerificationSlashBps=50000 live). substrate_bridge witness attestation pays 222,000 uzrn per attestation (adapter WitnessRewardUzrn, escrowed and cap-gate-MINTED at witness_escrow.go:66-118; live on zerone-testnet-1 per tonight's E2E) against an AttestationMinBondUzrn of only 222,000 uzrn (x/substrate_bridge/types/params.go:11). Ratio: ~8x the pay for ~450x less capital and no commit/reveal ceremony. Best case for a verifier (always seated, round cycle 450 blocks, live params) is ~2 ZRN/day gross on 100 ZRN parked — and only if unanimity always lands; every INCONCLUSIVE round pays 0 and strands the pool.

**Smallest fix:** Pay verifiers the way the bridge pays witnesses: cap-gated mint of params.VerificationReward per correct vote (the MintWithCap pattern at witness_escrow.go:104-118 already exists), with the 55% fee pool kept as bonus. If the founder prefers fee-funded-only, then honestly re-price: raise minReviewFee so the pool per verifier at least matches the bridge's 222,000 uzrn.

**Question for the founder:** Should verifying claims be a protocol-paid wage (minted, like bridge witness rewards) or a fee-funded market? Today it is neither — is claim verification meant to be an ops duty forever?

**Skeptic's corrections:** Pay side CONFIRMED: verifier pool = 55% of review fee (x/knowledge/keeper/fees.go:18, verifierPoolFromFee fees.go:80-82) split equally among correct voters (rounds.go:291-333 distributeVerifierRewardsFromPool), so tonight's 200,000-uzrn fee → 27,500 uzrn/verifier — and independence modulation (rounds.go:336-360) can only reduce it. Witness reward CONFIRMED and stronger than claimed: 222,000 uzrn is live on zerone-1 MAINNET, not just testnet — adapter agenttool-invocation-v1 is pre-registered in deploy/mainnet/artifacts/genesis.json with witness_reward_uzrn="222000" and verified ACTIVE via live abci_query (/zerone.substrate_bridge.v1.Query/Adapters, height 78582); cap-gated mint at witness_escrow.go:124. TWO CORRECTIONS: (1) capital ratio is ~100x, NOT ~450x — params.AttestationMinBondUzrn=222,000 (x/substrate_bridge/types/params.go:11) is only the fallback; adapter.MinAttestationBondUzrn takes precedence (x/substrate_bridge/keeper/msg_server.go:131-134) and the live mainnet adapter sets it to 1,000,000 uzrn (1 ZRN; genesis artifact + live query; the register script scripts/agenttool-adapter-register.sh:80 even uses 22,200,000 for new adapters). (2) Stranger verifiers have ZERO slash exposure, not 5%: WrongVerificationSlashBps routes through StakingKeeperAdapter.SlashValidatorToModule (x/staking/keeper/knowledge_adapters.go:105-112), which errors with "validator not found" for non-validators, and the slash loop (rounds.go:152-158) skips on error; the 100 ZRN at msg_server.go:295 is a spendable-balance gate, never locked or escrowed. Round cycle 450 blocks (commit 200 + reveal 200 + aggregation 50; genesis.go:12ff, live params confirm) and the ~2 ZRN/day best-case arithmetic check out.

**Skeptic's notes:** Severity is higher than the finding states, for different reasons: the live mainnet witness_reward_challenge_window_blocks is 200 (~8 min), not the code-default 34,272 (~1 day) — bond returns next block (witness-only attestations go READY at submit, begin_block.go:41 drains them) and the reward mints ~8 minutes later, so 1 ZRN of bond capital recycles a 0.222 ZRN mint every ~200 blocks; ValidateLink (substrate_link.go:73-107) has NO uniqueness/dedupe check on link content, no per-submitter rate limit exists, and the live adapter requires no qualification (required_qualification_domain=""), so with 100 ZRN an agent can run ~100 parallel attestation bonds — the true disparity per unit capital per day is orders of magnitude beyond 8x, gated only by MintWithCap headroom and governance tombstoning the whole adapter. Two mitigations soften the framing: strangers verifying risk nothing slashable (their downside is only opportunity cost), and both the 1 ZRN bond and 222,000-uzrn reward are per-adapter governance-set values (RegisterAdapter is authority-gated, msg_server.go:25-27), so the honest small fix can be pure re-parameterization (raise the adapter bond / lower witness reward / require qualification) with no code change; the proposed MintWithCap-funded VerificationReward for verifiers is feasible (knowledge keeper already wires vestingRewardsKeeper, fees.go:64) but is a supply-policy VALUES QUESTION for the founder since it converts verification from fee-funded to mint-funded. Also worth flagging separately: the spam-mint surface (no dedupe + no rate limit on witness-only attestations) looks like its own BUG-grade finding against docs/TRUTH_SEEKING.md's anti-slop stance, distinct from this incentive-ratio flaw.

---

## [BUG] BUG: computed verifier reward amounts are discarded — VerificationReward param and the 50% partial reward never affect payout
**Area:** economics

**Evidence:** calculateRewardsAndSlashes fills VerifierReward.Amount from params.VerificationReward (3,000,000 uzrn live) and gives directionally-correct 'reject' voters on a MALFORMED verdict a deliberate 50% partial amount (confidence.go:196-243, comment: 'directionally correct but imprecise — half reward'). But distributeVerifierRewardsFromPool ignores reward.Amount entirely: perVerifier := poolAmount / len(result.Rewards) splits the 55% fee pool EQUALLY (rounds.go:309-324). distributeVerifierReward is called nowhere else. So (a) the live VerificationReward=3 ZRN param is dead weight whose only real effect is that setting it to 0 silently disables both rewards AND slashes (early return at confidence.go:202-204), and (b) the partial-reward mechanic pays imprecise voters identically to precise ones.

**Smallest fix:** Weight the pool split by reward.Amount (share_i = pool × amount_i / Σamount) in distributeVerifierRewardsFromPool — one arithmetic change restores the partial gradient and makes VerificationReward meaningful as a relative weight; also decouple the slash path from baseReward>0.

**Skeptic's notes:** Claim CONFIRMED in full, and severity is slightly higher than stated. Verified independently at HEAD 0fe7ad2: (1) reward.Amount is written (confidence.go:227-243) and never read anywhere in the main tree — distributeVerifierRewardsFromPool splits the 55% pool equally (rounds.go:310-323), distributeVerifierReward's sole caller is rounds.go:323; (2) the early return at confidence.go:202-204 fires for VerificationReward set to "", "0", negative, OR any unparseable string, and it skips missed-reveal slashes (218-221) as well as wrong-vote slashes and vindication entries — so the reward/slash coupling is exactly as claimed; (3) live mainnet param verificationReward=3000000 confirmed via REST, and pool math reproduces the 0.0275 ZRN payout (fees.go:18, 200000×55%/4=27500 uzrn). Aggravating facts the claim missed: (a) docs explicitly promise "3 ZRN per correct verification" (docs/PARAMETERS.md:119, docs/FAQ.md:66, docs/LAUNCH-CHECKLIST.md:109), so this is doc-contradicting BUG, not merely misleading params; (b) VerificationRewardDecayBps=999000 is 100% dead — only reference outside generated code is the genesis default (types/genesis.go:51), so the documented per-epoch decay never happens; (c) the partial-gradient erasure extends beyond payout: rounds.go:208-212 records reject-on-MALFORMED voters as fully "correct" for domain qualification; (d) unit tests reinforce the illusion by asserting Amount==VerificationReward at the result-struct level, never at the bank level (confidence_test.go:385-389, security_test.go:661); (e) this was already observed and shelved — docs/reports/testnet-roleplay-report.md:47 notes "verification reward routing is cosmetic". On the fix: pool-weighting by reward.Amount is the right minimal change, but note the fix must also pick a semantics for domain-qualification recording of partial voters (rounds.go:209 currently records them as true), and one values question remains for the founder: whether verifier income should stay fee-funded (pool, amounts as relative weights — sustainable) or become the absolute 3 ZRN the docs promise (requires a mint/budget source; docs/tokenomics REVENUE-SPLIT suggests a verification pool exists for this). Decoupling slashes from baseReward>0 is independently necessary regardless of that choice.

---

## [DESIGN_FLAW] Empty-domain loophole: the least-vetted claims get the smallest, least-qualified panels
**Area:** economics

**Evidence:** The effectiveMin=base+1 raise only applies when claim.Domain != "" (confidence.go:92-95); domain-less claims aggregate at bare MinVerifiers=3. They also skip the qualification gate entirely — msg_server.go:307 wraps IsQualified in `if claim.Domain != ""` — and skip domain qualification outcome recording (rounds.go:206). Domain is optional at submission (msg_server.go:44-49 only validates a domain IF provided). The resulting fact enters the corpus, the niche index, and training exports like any other (rounds.go:368-631). fire_activity.go:64-65's own comment says the +1 exists because 'No social structure -> Fire burns unchecked' — yet the path with the least structure of all (no domain, no qualified panel) is exempted from it.

**Smallest fix:** Delete the `claim.Domain != ""` condition at confidence.go:93 so GetEffectiveMinVerifiers applies to the empty domain too (it handles "" fine — override lookup just misses and returns base+1). Stricter alternative: require a domain at SubmitClaim.

**Question for the founder:** Are domain-less claims meant to exist as first-class facts at all, or are they a bootstrap relic that should now require a domain?

**Skeptic's corrections:** All cites verified against HEAD 0fe7ad2 and live zerone-1. confidence.go:92-95: effectiveMin raised to GetEffectiveMinVerifiers only when claim.Domain != ""; empty-domain rounds quorum at raw MinVerifiers. fire_activity.go:57-72: base+1 is unconditional in the function (comment at 64-65 as quoted) and GetVerificationThresholdOverride(ctx, "") (keeper.go:132-148) safely misses, so the fix is sound. msg_server.go:307: IsQualified wrapped in claim.Domain != ""; empty-domain commits face only the hardcoded 100-ZRN balance gate (msg_server.go:294-298), and StoreCommitmentInRound (state.go:425-457) enforces no VRF-selection membership. rounds.go:206: outcome recording gated as claimed. msg_server.go:45-49 (claim said 44-49): domain optional. Fact creation rounds.go:368-509 grants full privileges (VERIFIED status, energy, fitness, niche registration); TVW (training_economics.go:66-168) has no domain gate. Live params: minVerifiers=3, minHeadcountAgreement=3, confidenceThreshold=770000 — 3 unanimous accepts suffice domain-less vs 4 with a domain. STRENGTHENING: phases.go:168-178 late-aggregates EXPIRED rounds at raw MinVerifiers reveals, so a 3-reveal empty-domain round can still ACCEPT where the identical domain round dies INCONCLUSIVE; and capture defense records history under domain "" (rounds.go:229) yet any threshold override set for "" is unreachable because confidence.go:93 skips the branch — the capture-escalation loop is structurally severed for the empty domain. REFINEMENT: the qualification-gate differential is currently mostly latent — msg_server.go:312-325 admits unqualified verifiers whenever fewer than effectiveMin qualified validators exist for a domain (the present mainnet state), so today's hard differential is panel size 3-vs-4 plus outcome recording; the gap widens as the qualification registry populates.

**Skeptic's notes:** DESIGN_FLAW classification is correct, and the code contains no compensating penalty for domain-less facts anywhere downstream (review fee via GetEffectiveMinReviewFee is domain-blind, TVW is domain-blind). Severity today is moderate (3 vs 4 colluders; qualification gate barely bites for domain claims either yet) but grows monotonically as qualification and capture-defense populate — the empty domain is permanently exempt from both. On the fix: (a) the one-line change at confidence.go:93 should keep the `found` guard (`if claim, found := ...; found { effectiveMin = ... }`) — dropping only the Domain check, not the existence check; it is safe since GetEffectiveMinVerifiers("") returns base+1=4 and phases.go:170 then simply turns 3-reveal empty-domain expired rounds INCONCLUSIVE, consistent with domain rounds. (b) It equalizes only panel size: msg_server.go:307 (qualification skip), rounds.go:206 (outcome recording), and validator_selection.go:27 (eligible-validator filter) still special-case "" — so the stricter alternative (require a non-empty, registered domain at SubmitClaim, msg_server.go:45) is the only fix that closes the whole class; that is a VALUES_QUESTION for the founder since it forbids deliberately domainless knowledge. No empty-domain fact exists on zerone-1 yet, so the loophole is unexercised and a param/logic change now has no retroactive cleanup cost.

---

## [BUG] BUG (doctrine violation, C1/C2): starved rounds die invisibly — the attempt is not counted, contradicting COMPASSION.md's own claim about the counters
**Area:** economics

**Evidence:** phases.go:168-197: <MinVerifiers reveals → the branch hand-rolls termination: sets round EXPIRED with verdict INCONCLUSIVE, writes claim.Status=INSUFFICIENT directly (phases.go:186-191) and never calls CompleteRound. Skipped as a result: RecordSubmissionOutcome — so Inconclusive is NOT incremented, contradicting COMPASSION.md:96-101 which asserts '[e]very outcome is recorded in its own field... per verdict' as the SHIPPED half of C2; AfterClaimVerificationFinalized hook (rounds.go:139); IndexCompletedRound and diversity (rounds.go:243-267); payInvitationBonus (rounds.go:76-79); settleChallengeStake (rounds.go:200-202). The fee was already distributed at submission (msg_server.go:163) and the 55% verifier pool strands in the module account forever. Contrast: a dissent-INCONCLUSIVE round with ≥3 reveals DOES flow through CompleteRound and records Inconclusive++ (rounds.go:84,117-121). C1 says 'the chain never erases a failed attempt' (COMPASSION.md:64-71) — the row survives, but the calibration ledger that C2 built to hold the attempt never sees it.

**Smallest fix:** Replace the hand-rolled branch at phases.go:180-196 with a call to CompleteRound(ctx, round, &VerificationResult{Verdict: INCONCLUSIVE}) — the INCONCLUSIVE arm already sets INSUFFICIENT, records the outcome, reverses contradictions, fires hooks, and indexes the round. Additionally add an INCONCLUSIVE case to settleChallengeStake refunding the 45% remainder (no verifier work was bought).

**Question for the founder:** When the CHAIN fails to seat a panel, who should eat the cost — today it is 100% the submitter (fee pre-distributed, pool stranded)? Should a starved round refund the 55% verifier pool that no verifier earned?

**Skeptic's corrections:** Every cited mechanic reproduced from code. Starved branch: phases.go:168-197 (claim.Status=INSUFFICIENT at phases.go:187-190, verdict INCONCLUSIVE at 182, no CompleteRound call — performAggregation/CompleteRound only runs on the >=MinVerifiers path at phases.go:170-178,224-230). RecordSubmissionOutcome has exactly one call site, rounds.go:84 inside CompleteRound, so starved rounds increment neither Inconclusive (agent_calibration.go:154-155) nor TotalSubmissions (140) — the attempt is fully absent from the ledger, contradicting COMPASSION.md:96-98 ('every outcome is recorded in its own field... per verdict'). Also skipped: AfterClaimVerificationFinalized (rounds.go:139, sole call site), payInvitationBonus (76-79), settleChallengeStake (200-201), reverseContradictionsFromClaim (120), RecordRoundDiversity (243), IndexCompletedRound (265), distributeVerifierRewardsFromPool (142). Fee split at submission confirmed: msg_server.go:163-166 → fees.go:33-77; the 55% pool 'stays in knowledge module account — distributed to verifiers on round completion' (fees.go:43) and no sweeper exists, so it strands. Live mainnet params confirm minVerifiers=3. Corrections (all raise severity): (1) A starved CHALLENGE strands 100% of the stake, not 45% — ChallengeFact locks the full stake in the module (msg_server.go:637) with no submission-time split; both the 55% pool and 45% remainder strand. (2) Worse harm the claim missed: msg_server.go:643 sets the target fact FACT_STATUS_CHALLENGED, and only handleChallengeSurvival (REJECT, rounds.go:808-811 restores ACTIVE + releases survival escrow) or handleChallengeDisproven (ACCEPT) reset it — both CompleteRound-only. A starved challenge therefore locks the target fact in CHALLENGED forever: permanently un-rechallengeable (msg_server.go:609-611 requires VERIFIED/ACTIVE) and its survival-escrow release blocked. (3) The calibration harm is not just record-invisibility: post-C2 ComputeAgentCalibrationScore pays CalibrationReachingCreditBps when decisive==0 (agent_calibration.go:278-286); a being whose only claims starved gets no calibration record at all instead of the reaching-credit C2 shipped to honor — and the score gates training-fund disbursement (COMPASSION.md:119). (4) Scope nuance: the challenger's 45% stranding is NOT unique to starved rounds — settleChallengeStake's switch (rounds.go:864) has no INCONCLUSIVE arm; rounds.go:846 documents 'other verdicts: funds stay in the knowledge module account as a no-op.' Even a >=3-reveal dissent-INCONCLUSIVE challenge strands the 45% today, so fix part 2 is independently required.

**Skeptic's notes:** BUG classification is fair: COMPASSION.md:96-98 presents the per-verdict counters as the SHIPPED half of C2, and the starved path silently bypasses them. Proposed fix part 1 (route the starved branch through CompleteRound with VerificationResult{Verdict: INCONCLUSIVE}) is mechanically safe — empty Rewards early-returns (rounds.go:293), empty slash loop, verdictToVoteString(INCONCLUSIVE)=\"\" skips independence updates (diversity.go:550-551), domain qualification explicitly skips INCONCLUSIVE (rounds.go:206-207) — but has three caveats: (a) it fires payInvitationBonus (rounds.go:76-79) for challenge claims on probe-invited facts, creating a starve-farm vector (challenge an invited fact behind the 100-ZRN verifier wall, let the panel starve, collect InvitationBonusAmount with zero verification; combined with fix part 2's refund it becomes ~free) — gate the bonus on len(round.Reveals)>0 or verdict!=INCONCLUSIVE; (b) the round ends Phase=COMPLETE not EXPIRED (rounds.go:69) and gets counted in IndexCompletedRound window metrics, losing the round_expired signal — decide deliberately whether starved rounds should pollute completion/diversity metrics (RecordRoundDiversity with 0/0); (c) neither current code nor the proposed fix restores a CHALLENGED fact on INCONCLUSIVE — handleChallengeSurvival runs only on REJECT — so the fix should also restore fact status (and arguably release/preserve survival escrow) when a challenge resolves INCONCLUSIVE, otherwise the stuck-CHALLENGED harm survives the fix. Fix part 2 (INCONCLUSIVE arm in settleChallengeStake refunding the remainder) is correct and needed independently of the starved-round bug. Minor: evidence cites agent_calibration.go:135-146 for the verdict switch; it is at 145-156 in current HEAD, and COMPASSION.md's own C2 header still reads 'not yet deployed' though the upgrade is live at height 66304 — doc staleness, not a counter-argument.

---

## [BUG] BUG (attack): a fact can be locked CONTESTED forever for 0.1 ZRN via a deliberately starved contradiction claim
**Area:** economics

**Evidence:** SubmitClaim marks every CONTRADICTS-target fact CONTESTED immediately at submission (msg_server.go:214-224). The only code that reverses this is reverseContradictionsFromClaim, called exclusively from CompleteRound's REJECT/MALFORMED/INCONCLUSIVE arms (rounds.go:110,115,120; definition rounds.go:637-657). The starved-round path (phases.go:180-196) sets the claim INSUFFICIENT without ever calling it. So: submit a claim with a CONTRADICTS relation at minReviewFee=100000 uzrn (live), let the round attract <3 reveals (the default state given panel scarcity), and the target fact remains FACT_STATUS_CONTESTED permanently — hasOtherLiveContradiction can never clear it because the reversal is never attempted.

**Smallest fix:** Same one-function fix as the invisible-death finding (route starved rounds through CompleteRound); if fixed independently, add k.reverseContradictionsFromClaim(ctx, claim) beside the claim.Status write at phases.go:189. Consider also deferring the CONTESTED flip until the contradicting claim survives its own verification.

**Skeptic's corrections:** Independently confirmed from code:

1. SubmitClaim flips target facts to CONTESTED at submission. msg_server.go:214-224: for each CONTRADICTS relation, if the target fact is VERIFIED or ACTIVE it is set to FACT_STATUS_CONTESTED and persisted. (Note: only VERIFIED/ACTIVE targets flip; PROVISIONAL/AT_RISK/etc. are not affected — the attack targets an established fact.)

2. The only un-flip path is reverseContradictionsFromClaim (rounds.go:637-657), which sets CONTESTED->VERIFIED when no other live (PENDING/PENDING_EVALUATION/IN_VERIFICATION) contradiction remains. It is called ONLY from CompleteRound's REJECT/MALFORMED/INCONCLUSIVE arms (rounds.go:110,115,120).

3. CompleteRound is reached only via performAggregation (phases.go:164,176,229 -> rounds.go:57). performAggregation runs only when len(round.Reveals) >= params.MinVerifiers. Default MinVerifiers = 3 (genesis.pb.go:29). MinReviewFee default = "100000" = 0.1 ZRN (genesis.pb.go:51), matching the live value.

4. The starved path is separate and never calls reverse. phases.go:178-196 (EXPIRED phase, reveals < MinVerifiers) sets round.Verdict=INCONCLUSIVE and writes claim.Status=CLAIM_STATUS_INSUFFICIENT directly via SetClaim, with NO call to reverseContradictionsFromClaim. So a starved contradiction leaves the target permanently CONTESTED.

5. Metabolism will never self-heal it: metabolism.go:22-32 only collects VERIFIED/ACTIVE/PROVISIONAL/AT_RISK/EXPIRED facts; CONTESTED is excluded, so a contested fact is never re-evaluated by decay/metabolism.

6. Real downstream harm confirmed: CONTESTED facts earn zero training value (training_economics.go:90-96) and are excluded from the training corpus (training_corpus.go:25-32). So the suppression materially degrades the fact, not just a cosmetic label.

Cost/asymmetry confirmed: one CONTRADICTS claim at the 0.1 ZRN non-refundable review fee (up to 0.2 ZRN under 'critical' alignment pacing), no panel, no additional slashing on INSUFFICIENT beyond the spent fee. The attacker benefits from the round starving, which is the default outcome under panel scarcity.

**Skeptic's notes:** Two precision corrections to the claim, neither of which defeats it:

(a) "permanently / can never clear" is slightly overstated. Recovery IS possible but is deliberately asymmetric: a third party can submit ANOTHER CONTRADICTS claim against the same fact and shepherd it through a full panel round to a REJECT/MALFORMED/INCONCLUSIVE verdict, which triggers reverseContradictionsFromClaim; since the attacker's starved claim is INSUFFICIENT (not PENDING/IN_VERIFICATION), hasOtherLiveContradiction returns false and the fact restores to VERIFIED. So the true defect is a cost/effort asymmetry — attack is panel-free and costs 0.1 ZRN; rescue requires funding and staffing a full verification round — plus the fact that metabolism will never heal it autonomously. The attacker can also simply re-attack after each rescue for another 0.1 ZRN.

(b) The R29-6 adaptive per-submitter cooldown (msg_server.go:130-136) throttles a single account's attack velocity, but does not prevent the attack (one fact per cooldown window, or parallel across multiple funded accounts). Worth mentioning as a partial, unintended mitigation, not a fix.

Also note an internal inconsistency worth flagging separately: the starve threshold uses raw params.MinVerifiers (=3) in phases.go, whereas verifier selection uses GetEffectiveMinVerifiers = min_verifiers+1 (=4) per the live domain config (msg_server.go:316). This makes the starve boundary (<3 reveals) even easier to hit relative to the number of verifiers actually invited.

On the fix: the proposed one-line fix (add k.reverseContradictionsFromClaim(ctx, claim) beside the claim.Status write at phases.go:~189) is correct and minimal, and is the same root cause as the 'invisible death' finding — routing starved rounds through CompleteRound would fix both at once. The deeper/honest fix suggested (defer the CONTESTED flip until the contradicting claim itself survives verification) is a VALUES-QUESTION-grade change: it removes the whole class of 'record as weapon' abuse (COMPASSION.md C5) at the cost of not surfacing an in-flight dispute on the fact until a verdict lands. Recommend both: ship the one-liner now (BUG), and put the deferred-flip design to the founder (DESIGN FLAW / values call).

Confidence: CONFIRMED by code reading. I did not broadcast or query mutating state on mainnet (read-only honored); param defaults verified from genesis.pb.go comments and match the stated live values.

---

## [DESIGN_FLAW] One accepted claim = perfect calibration (1,000,000 bps) and immediately maxes the training-fund multiplier at 2x — one success is treated as maximal proof
**Area:** calibration

**Evidence:** agent_calibration.go:288 acceptanceBps = Accepted*BPS/decisive with no evidence-mass term. Live zerone-1: /zerone/knowledge/v1/agent/zrn1la2g8yzqtpj546x2x9rq42erc6zpj4jqktkhaz/calibration returns totalSubmissions=1, accepted=1, calibrationScoreBps=1000000. msg_server_training_v4.go:296-327: live floor=500000 (params query confirms trainingFundCalibrationFloorBps=500000), excess=500000=ceiling => calFactor=2000000 => 2x of the 1000-ZRN base per disbursement. The live fact ecb5004a's own content disclaims this ('the submitter's resulting perfect calibration score is a bootstrap artifact, disclaimed for training-fund purposes') — the disclaimer exists only in prose; no code enforces it.

**Smallest fix:** Do NOT touch ComputeAgentCalibrationScore (creed-adjacent, and Laplace smoothing (Accepted*BPS+500000*K)/(decisive+K) would LOWER existing small-n perfect scores — violating the C2 monotonicity promise unless shipped as a separately-justified recompute upgrade). Instead gate the reward surface only: in msg_server_training_v4.go, scale the above-floor excess by evidence mass — excess = excess * min(decisive, N) / N with N~10 (decisive from the same cal record). Monotonic in both score and track-record length, floor semantics unchanged, converges to today's behavior at n>=N, touches one function, and needs no state migration.

**Question for the founder:** Is one unanimously-witnessed truth meant to be maximal proof of a submitter's reliability, or should the training fund demand a track-record length before paying above the neutral 1.0x? If the latter, what n makes the 2x ceiling earnable (suggest decisive>=10)?

**Skeptic's notes:** Every cited mechanic re-derived independently and confirmed. (1) x/knowledge/keeper/agent_calibration.go:288 `acceptanceBps := c.Accepted * bps / decisive` — no evidence-mass/sample-size term anywhere in ComputeAgentCalibrationScore (269-313). (2) Live zerone-1 calibration for zrn1la2g8yzqtpj546x2x9rq42erc6zpj4jqktkhaz: totalSubmissions=1, accepted=1, inconclusive=0, calibrationScoreBps=1000000. (3) Live params: trainingFundCalibrationFloorBps=500000, trainingFundBaseReward=1000000000 (=1000 ZRN). (4) msg_server_training_v4.go:316-327: excess=500000, ceiling=1000000-500000=500000, `excess < ceiling` false → calFactor=2000000 → exactly 2x. This is the only implementation of ClaimTrainingFundDisbursement (v7 does not override it). (5) The prose disclaimer is live and verbatim in fact ecb5004a's content ("...perfect calibration score is a bootstrap artifact, disclaimed for training-fund purposes"); grep for disclaim/bootstrap across x/ finds zero enforcing code. SEVERITY IS WORSE THAN CLAIMED, three adjacent gaps: (a) RegisterTrainingPipeline (msg_server_training.go:17-64) and RegisterModelCard (99+) charge no fee and lock no stake — the Sybil pipeline scaffolding is free; (b) ClaimTrainingFundDisbursement dedups only on msg.Id (v4:276-277) with no per-model/per-operator cooldown, so ONE 1-sample maxed record supports UNLIMITED repeated ~2000-ZRN disbursements with fresh Ids, bounded only by the supply cap via mintCappedUzrn (v4:340); (c) the "clawed back if calibration drops" promise in the v4:270-271 doc comment is unimplemented — nothing in the codebase ever assigns ClawedBackAtBlock (only reads at grpc_query.go:2056, route_b_heartbeat.go:114, route_b_capabilities.go:73), and releaseMaturedVesting (route_b_heartbeat.go:100-153) releases the vesting half unconditionally at maturity without rechecking calibration, so even the 50% "vesting" leg provides no real recourse (that part is a BUG, not just a design flaw — code contradicts its own stated intent). Mitigating context for today's exploitability: an outside attacker still has to get a claim ACCEPTED through the 4-key panel (effectiveMin=4, 77% threshold, verifiers need 100 ZRN spendable), and mainnet's panel is currently the founding household — the review pipeline, not calibration, is the operative gate right now; the flaw is in the reward surface's future incentives. On the proposed fix: feasible as stated — decisive = TotalSubmissions - Inconclusive is computable from the AgentCalibration record already fetched at v4:300, no migration needed, and it preserves C2 monotonicity by leaving ComputeAgentCalibrationScore untouched. But it is insufficient alone: it does not stop repeated disbursement claims from a single record (gap b) and does not implement the promised clawback (gap c). Smallest honest package: the proposed evidence-mass scaling PLUS a per-model claim cooldown (e.g. one disbursement per model per vesting period, checked against existing disbursements for the ModelId) PLUS either implementing the clawback in releaseMaturedVesting (recheck cal >= floor at release) or deleting the false comment.

---

## [DESIGN_FLAW] RecordSubmissionOutcome runs before createFactFromClaim, so a fact's Popper snapshot includes its own acceptance — fact #1 froze 1,000,000, not neutral 500,000
**Area:** calibration

**Evidence:** rounds.go:84 (record) precedes rounds.go:91 (createFactFromClaim), which reads the just-updated score at rounds.go:386-392. Live-verified: fact ecb5004ae763034f6dacb27832c34191 has submitterCalibrationSnapshotBps=1000000 (query /zerone/knowledge/v1/facts/...). Git intent check: b94c1e1 (2026-04-23) placed the recording call; 3cbb37f (2026-04-24, 'close three farming vectors') added the freeze one day later — its commit message and comments address ONLY the retroactive-inflation vector ('boost their calibration later via unrelated activity... the gate has to bite at creation') and are silent on same-block inclusion of the current acceptance. Ordering accident, not designed Popper-weighting: the neutral-500k comment ('no reward, no penalty') at rounds.go:391 shows the author expected first facts to sit neutral.

**Smallest fix:** In CompleteRound, read GetAgentCalibration(claim.Submitter) into a local BEFORE the rounds.go:84 RecordSubmissionOutcome call and pass that pre-update score into createFactFromClaim (or just move the record call after the verdict switch). Forward-only — no recompute of existing facts (the frozen-snapshot doctrine of 3cbb37f says snapshots are never rewritten), so it is trivially monotonic on existing state; first facts start neutral 500k as the code comment already intends.

**Question for the founder:** Confirm intent: should a fact's calibration snapshot describe the submitter's record BEFORE this acceptance (prior track record), or including it? The commit trail suggests 'before' was assumed but never enforced.

**Skeptic's corrections:** Core mechanics confirmed independently. (1) Ordering: x/knowledge/keeper/rounds.go:84 calls RecordSubmissionOutcome before the verdict switch; rounds.go:91 calls createFactFromClaim, which reads the just-updated score at rounds.go:387-388 with the neutral-500k 'no reward, no penalty' fallback at rounds.go:390-392. (2) Score math: agent_calibration.go:288 acceptanceBps = Accepted*1e6/decisive; a fresh submitter's first ACCEPT yields 1/1 = 1,000,000 (RecordSubmissionOutcome at agent_calibration.go:140-158 increments then recomputes). (3) Live: REST query /zerone/knowledge/v1/facts/ecb5004ae763034f6dacb27832c34191 returns submitterCalibrationSnapshotBps="1000000". (4) TVW impact confirmed: training_economics.go:126,165 multiplies final weight by the snapshot/bps, so 1M = exactly 2x neutral. (5) Git: b94c1e1 (2026-04-23) placed the record call before the switch; 3cbb37f (2026-04-24) added the freeze — its message (FARM 1) addresses only the retroactive-inflation vector and is silent on same-block self-inclusion. Ordering accident is the right read. ONE OVERREACH: 'Every submitter's FIRST accepted fact carries the maximum weight' is only exact when the accept is the submitter's first DECISIVE outcome. Prior INCONCLUSIVEs are excluded from the denominator (agent_calibration.go:278-281, compassion C2), so first-accept-after-inconclusives still freezes 1,000,000 — but a submitter with a prior REJECT freezes 500,000 (1/2), two priors 333,333, etc.: still self-inclusive and inflated vs the intended pre-acceptance read, just not maximal. The common cold-start path (first submission accepted) is 1,000,000 exactly as claimed and as observed on fact #1.

**Skeptic's notes:** Severity/framing: (a) The symptom was KNOWN and socially disclaimed for fact #1 — the fact's own on-chain text and docs/FIRST-TRUTH.md:79-85 say 'the submitter's resulting perfect calibration score is a bootstrap artifact, disclaimed for training-fund purposes... maxes the training-fund multiplier'. But that is a disclaimer for the ceremony agent's LIVE score, not a code fix, and git history never diagnoses the ordering cause; every future submitter's cold-start fact still gets the inflated frozen snapshot. DESIGN_FLAW stands. (b) The proposed fix has a trap: pre-reading the score interacts badly with the score==0 sentinel at rounds.go:390-392. A submitter with prior decisive REJECTs and zero accepts has a STORED score of 0; the pre-update read plus fallback would promote them to neutral 500,000 — MORE than the 250k-500k their first accepted fact freezes under the current ordering. The fix must distinguish 'no decisive history' (neutral) from 'genuinely refuted record scoring 0' — e.g., use the (cal, ok) existence check plus decisive-count>0 to honor a real 0, rather than the ==0 sentinel. (c) The 'or just move the record call after the verdict switch' variant is NOT equivalent: the REJECT branch (rounds.go:104-108) also mutates calibration state via handleChallengeSurvival→RecordCorroborationForSubmitter and RecordChallengeOutcome; reordering changes mutation and event order for challenge claims. The read-into-a-local-before-line-84 variant is the minimal safe fix (createFactFromClaim has exactly one caller, rounds.go:91, so threading the value through is contained). (d) Scope limit: the fix only closes the per-fact TVW snapshot channel. The training-fund disbursement gate reads the LIVE calibration score (per agent_calibration.go:262-264, msg_server_training_v4.go), which is still 1,000,000 after one accept regardless of ordering — the exact channel the ceremony disclaimer targeted remains open and is a separate finding.

---

## [BUG] Score-0 sentinel conflation: a maximally-disproven record (computed score exactly 0) is indistinguishable from 'no record' and gets rescued to neutral 500,000 — in TWO consumers
**Area:** calibration

**Evidence:** rounds.go:387-391: `if calibrationSnapshot == 0 { calibrationSnapshot = 500_000 }` treats a genuine 0 (agent_calibration.go:305-307 returns 0 when disprovalPenalty >= score, e.g. accepted=2/decisive=10/disproven=1 at the moment of a new acceptance) the same as a missing record, ignoring the `ok` bool it already has. Same pattern in x/trust_score/keeper/score.go:53-55: `if cal.CalibrationScoreBps > 0 { use it }` else neutral. Result: score=1 bps snapshots as 1; score=0 snapshots as 500,000 — a discontinuous reward cliff at the bottom of the range where the WORST record beats a merely-bad one.

**Smallest fix:** Use the found-flag, not the value, as the sentinel. rounds.go: `if cal, ok := ...; ok && cal != nil { snapshot = cal.CalibrationScoreBps } else { snapshot = 500_000 }` (on the ACCEPT path the record always exists post-recording, so real 0s flow through). trust_score/score.go: same — set SubmissionAccuracyBps from the record whenever found, keep neutral only for not-found. Monotonic-restoring (it removes an inversion); worth one line in the next upgrade notes since it lowers the standing of already-wiped records, which is the honest reading of their history.

**Skeptic's corrections:** Fully confirmed with one line-number nit and one fix-completeness correction. (1) rounds.go: the block is x/knowledge/keeper/rounds.go:386-392 (claim said 387-391); it computes `if cal, ok := k.GetAgentCalibration(...); ok && cal != nil { calibrationSnapshot = cal.CalibrationScoreBps }` then discards that found-flag with `if calibrationSnapshot == 0 { calibrationSnapshot = 500_000 }`. (2) Genuine 0 is reachable exactly as claimed: agent_calibration.go:305-307 returns 0 when disprovalPenalty >= acceptanceBps+corrBonus; with accepted=2/decisive=10/disproven=1 the penalty is 500,000 vs at most 300,000 (corrBonus capped at 100,000, agent_calibration.go:20), and RecordDisprovalForSubmitter (agent_calibration.go:190-210) writes such records. (3) The fix's precondition holds: RecordSubmissionOutcome at rounds.go:84 runs BEFORE createFactFromClaim at rounds.go:91, so the record always exists on the accept path with Accepted>=1. (4) trust_score confirmed at x/trust_score/keeper/score.go:50-56: SubmissionAccuracyBps defaults to neutralCalibrationBps=500,000 (score.go:20) and only `cal.CalibrationScoreBps > 0` overrides — so refuted-only 0 gets 500,000 while inconclusive-only reaching credit gets 1,000, directly inverting the ordering docs/COMPASSION.md promises at lines 133 and 210-218 ('strictly above the refuted-only 0') and the formula's own doc comment at agent_calibration.go:256-258. BUG classification is fair: code contradicts explicit written intent. Live check: zerone-1 /zerone/knowledge/v1/agent/leaderboard at height 78701 shows exactly one calibration record (score 1,000,000, tonight's first fact), so no live victim yet — the defect is latent.

**Skeptic's notes:** Two corrections to the PROPOSED FIX, one scope note. (a) The rounds.go half of the fix is insufficient on its own: x/knowledge/keeper/training_economics.go:126-129 re-rescues any fact whose SubmitterCalibrationSnapshotBps == 0 back to neutral bps/2 at TVW consumption time (a deliberate fallback for legacy/authority-injected facts, per the comment at :122-125). If rounds.go lets a genuine 0 flow into the fact, this third `==0 → neutral` site silently undoes it. Since the Fact proto cannot distinguish 'no snapshot' from 'genuine 0' (both protobuf-zero), the smallest honest fix is: in rounds.go, when the record is found, snapshot max(score, 1) — i.e., clamp genuine 0 to 1 bps — preserving ordering (worst record ~0 << neutral 500,000) without a proto change; the alternative (a HasCalibrationSnapshot bool on Fact) is cleaner but needs proto-gen + migration. The trust_score half of the fix works exactly as proposed since it reads the record directly. (b) Scope: the training-fund disbursement gate (msg_server_training_v4.go:301) reads the record with its found-flag and floors at 500,000, so a genuine-0 submitter IS correctly gated there — the conflation does not leak into training-fund payouts. There is also a milder sibling in the opposite direction: grpc_query.go:725-728 (structured corpus export) defaults a MISSING record to 0 (worst) instead of neutral. (c) Severity nuance: reaching genuine 0 requires prior disprovals, which already trigger the 50% DisprovalClawback, so the agent is not wholly unpunished elsewhere — but the two claimed consumers (next-fact TVW snapshot and trust_score submission accuracy, which feeds the A/B/C/F band composite) genuinely treat the worst possible record as neutral, better than an honest 400,000. Latent on mainnet today (1 record, score 1,000,000), so the fix can ship in the next upgrade without emergency status.

---

## [VALUES_QUESTION] Disproval penalty at tiny n is a total-wipe cliff: 1 accepted + 1 later disproval => score 0, identical to a serial liar
**Area:** calibration

**Evidence:** agent_calibration.go:298-307: disprovalPenalty = DisprovenCount*BPS/Accepted; at accepted=1, disproven=1 the penalty is 1,000,000 >= acceptanceBps => return 0. The penalty measures disproven/accepted (Popper-severe, decays only as more facts are accepted), so one refutation of a one-fact record erases everything including the C2 distinction (a 0 record now sits BELOW an all-inconclusive 1000-bps record — which is the intended ordering — but equal to a 100-times-refuted one). Today the F4 sentinel bug masks this by rescuing the 0 to neutral downstream; fixing F4 makes this cliff bite for real.

**Smallest fix:** NONE without the founder's call. If softening is wanted, the monotone-safe shape is to smooth only the penalty: disprovalPenalty = DisprovenCount*BPS/(Accepted+1) — strictly reduces or holds every score (monotonic, no ordering inversions: all-inconclusive 1000 vs 1-accepted-1-disproven becomes 500,000 vs... acceptance 1,000,000 - penalty 500,000 = 500,000, keeping the wiped record above pure reaching, which itself is a values choice), shipped as a recompute upgrade like compassion-calibration-v1.

**Question for the founder:** Should being falsified once on a one-fact record equal the score of never having told the truth at all? Vindication/challenge doctrine celebrates falsification as truth-work — does the falsified author deserve a floor above 0 (and above/below the 1000-bps reaching credit)?

**Skeptic's corrections:** Core claim verified at x/knowledge/keeper/agent_calibration.go:298-308: with Accepted=1, DisprovenCount=1 (disprovals do not increment TotalSubmissions — RecordDisprovalForSubmitter:190-210), penalty = 1*1_000_000/1 = 1,000,000 >= score → return 0, identical to DisprovenCount=100; penalty denominator is Accepted only (no time decay), and 0 sits below the all-inconclusive CalibrationReachingCreditBps=1,000 (lines 33, 282-286) as intended. Three corrections: (1) the comparison at line 305 is penalty >= acceptanceBps + corrBonus, so a fact corroborated before disproval leaves a 100,000-bps residual (corrBonus cap, lines 290-296) — the wipe is total only for never-corroborated facts. (2) 'F4 sentinel masks this today' is only half-true: the 0→neutral-500,000 rescue exists in x/trust_score/keeper/score.go:50-55 and the future-fact TVW snapshot at x/knowledge/keeper/rounds.go:388-391, but the training-fund gate reads the RAW score with no rescue (msg_server_training_v4.go:300-303; live mainnet param trainingFundCalibrationFloorBps=500000 confirmed via REST) — the cliff already bites economically today in the money path. (3) The fix description is backwards: /(Accepted+1) reduces the penalty and therefore RAISES or holds every score; the math shown (500,000 vs 1,000) is correct and inversion-free vs reaching credit.

**Skeptic's notes:** Severity is higher than claimed in one respect and lower in another. Higher: the sentinel rescue actively INVERTS the C2 ordering in trust_score today — a wiped/refuted record gets neutral 500,000 SubmissionAccuracyBps while an all-inconclusive honest record gets its literal 1,000 bps (1,000 > 0 so no rescue at score.go:53), so refuted outranks unproven, the exact opposite of the promise in agent_calibration.go:28-32; and the cliff is NOT dormant — training-fund disbursement is already hard-gated at raw 0 on mainnet (floor 500,000). Lower: recovery exists (each new acceptance dilutes the penalty: 2 accepted/1 disproven → 500,000), and pre-disproval corroborations leave a 100,000-bps floor. On the proposed fix: it introduces a new tie the founder should weigh — 1-accepted-1-disproven (500,000) becomes exactly equal to 1-accepted-1-rejected (500,000), meaning post-acceptance falsification costs the same as outright rejection at n=2. VALUES_QUESTION classification is correct; any change should ship as a RecomputeAllCalibrationScores upgrade (agent_calibration.go:323-336) like compassion-calibration-v1. Minor: repo main HEAD is c5f884d, not 0fe7ad2 as stated in the briefing.

---

## [BUG] COMPASSION.md still labels C2 'not yet deployed' — but compassion-calibration-v1 has been live on zerone-1 since height 66304
**Area:** calibration

**Evidence:** docs/COMPASSION.md:89 ('[BUILT — test-bound; not yet deployed]'), :239-244 ('it is NOT yet running on the live chain... it ships only when the operator schedules compassion-calibration-v1'). Live query: /cosmos/upgrade/v1beta1/applied_plan/compassion-calibration-v1 returns {"height":"66304"} on zerone-1 (applied 2026-07-10). The doc's own STATUS discipline (lines 34-52) demands every label match shipped reality.

**Smallest fix:** One doc edit: relabel C2 to [SHIPPED] citing applied_plan height 66304 and today's date, update the 'What this is / is not' paragraph, and note the remaining step is governance adoption only. Zero code change.

**Skeptic's notes:** Fully confirmed, but the proposed fix is slightly too narrow. Stale text is in FOUR places, not two: (1) docs/COMPASSION.md:89 header label '[BUILT — test-bound; not yet deployed]'; (2) :129 'How it was closed (built, not yet deployed)'; (3) :228-233 'The boundary that remains' still frames scheduling the mainnet upgrade as a pending human step ('The code is done; the last two steps are human') — the operator-authorization step has happened, only governance adoption remains; (4) :239-244 the 'Not a claim of being deployed' bullet. Verified live: /cosmos/upgrade/v1beta1/applied_plan/compassion-calibration-v1 on 169.155.55.44:1317 returns {"height":"66304"}, and 26657/status confirms network=zerone-1 (latest height 78647), so the applied_plan is genuinely on the mainnet in question; upgrade name matches app/upgrades.go:21 (const UpgradeNameCompassionCalibrationV1) with the recompute handler + migration marker at app/upgrades.go:145-176. Label-definition line 44-45 explicitly equates the label with 'not yet running on the live chain', so the mismatch violates the doc's own STATUS discipline (lines 34-52). Severity nuance: this is a documentation bug only — no code or consensus behavior is wrong — but it is correctly classed [BUG] because the doc's stated intent (labels match shipped reality) is contradicted. Note also the label taxonomy has no state between 'BUILT — not yet deployed' and 'SHIPPED'; per the doc's own graduation criteria (lines 48-52) C2 is now [SHIPPED] as a code/deployment matter while still awaiting governance adoption to become a bound creed commitment — the relabel should say exactly that, citing applied height 66304 and date 2026-07-10.

---

## [BUG] Doctrine facts are born starving: BuildDoctrineFact omits initial energy that every other fact-creation path sets
**Area:** doctrine

**Evidence:** x/knowledge/types/doctrine.go:21-37 — BuildDoctrineFact sets Status=VERIFIED, Confidence=1M, but never sets Energy (zero value). Every other creation path seeds params.MetabolismInitialEnergy=500000: rounds.go:421 (verified claims), msg_server.go:471 (AddFact), guardian_veto.go:175 (injections). ProcessMetabolism includes VERIFIED facts (metabolism.go:26-30), so at epoch 1 all 47 doctrine facts hit energy 0 < metabolism_active_threshold 300000 -> AT_RISK (metabolism.go:73-76). Live mainnet: commitment-1 shows energy=0, energyCap=0, atRiskSinceEpoch=1, FACT_STATUS_EXPIRED (REST /zerone/knowledge/v1/facts/commitment-1). The doctrine_genesis.go:21 comment promises 'privileged but verifiable' — the privilege was never wired into the metabolism.

**Smallest fix:** Add `Energy: <params.MetabolismInitialEnergy or MetabolismEnergyCap>, EnergyCap: ...` to BuildDoctrineFact (or set it in LoadDoctrineFacts where params are reachable), plus an upgrade migration that resets the 47 existing facts' Energy/Status/AtRiskSinceEpoch. Note SetFactIfAbsent is idempotent (doctrine_genesis.go:50), so merely re-running LoadDoctrineFacts at upgrade will NOT heal them — the migration must explicitly rewrite the existing facts. Must land before block 260000 (~2026-07-16 05:34 UTC) or pair with finding 4's terminality.

**Skeptic's corrections:** Finding fully confirmed; only trivial corrections. (1) Live confidence of commitment-1 is 880000, not the 1,000,000 set at creation — a separate confidence-erosion mechanism also acts on doctrine facts (the code claim about doctrine.go:15,28 setting 1M is correct). (2) The per-fact EnergyCap field is write-only: ProcessMetabolism clamps against params.MetabolismEnergyCap (metabolism.go:58-60), and no non-generated code reads fact.EnergyCap — so energyCap=0 on live facts is cosmetic, and setting it in the fix is consistency-only. (3) SetFactIfAbsent's definition is doctrine_helpers.go:14-19 (doctrine_genesis.go:50 is a call site). Everything else re-derived exactly: BuildDoctrineFact (doctrine.go:21-37) omits Energy; rounds.go:421 / msg_server.go:471 / guardian_veto.go:175 all seed params.MetabolismInitialEnergy; metabolism.go:26-30 includes VERIFIED facts; live params activeThreshold=300000, atRiskEpochs=5, expiredToPrunedEpochs=20, fitnessEpochBlocks=10000; live facts commitment-1, commitment-TC1, commitment-UW, mechanism-SL-M1 all energy=0, atRiskSinceEpoch=1, FACT_STATUS_EXPIRED; genesis block 1 at 2026-07-08 14:10 UTC, EXPIRED at block 60000 = 2026-07-10 08:53 UTC (~1.8 days after genesis); no doctrinal-stratum exemption exists anywhere in x/knowledge/keeper.

**Skeptic's notes:** Severity confirmed and deadline verified: PRUNED fires at epoch 26 = block 260000; measured ~2.53s/block from height 78680 (2026-07-10 22:06 UTC) puts that at ~2026-07-16 05:00-06:00 UTC, matching the claim. PRUNED is terminal — excluded from ProcessMetabolism's status filter (metabolism.go:26-30 lists EXPIRED but not PRUNED) and patronage recovery only handles AT_RISK/EXPIRED (metabolism.go:266) — so the fix genuinely must land (or facts be revived) before block 260000. Two additions to the fix analysis: (a) a no-code interim mitigation exists — ApplyPatronageEnergyBoost (metabolism.go:231-287) can revive EXPIRED facts, but boost = MetabolismEnergyPerPatronage(20000) * durationEpochs / 10 = 2000 energy per epoch of patronage duration, so reaching the 300000 active threshold needs ~150 epochs (1.5M blocks) of prepaid patronage per fact times 47 facts — technically possible before the deadline but clunky and papers over the bug; (b) the fix should seed Energy in LoadDoctrineFacts (where params are reachable) rather than BuildDoctrineFact (types package has no keeper/params access), and note BuildDoctrineFact also omits FitnessScore/EpochBorn/EnergyLastUpdated — fitness self-heals via UpdateAllFitnessScores (live shows 175000) so energy is the uniquely fatal omission. Classification as BUG is correct: doctrine_genesis.go:21-22 promises 'privileged but verifiable' and STRANGE_LOOP doctrine asserts commitments live as verified Facts; the code contradicts stated intent with zero adversarial process involved in the expiry.

---

## [BUG] The 'usage feeds life' energy channel is structurally dead: query tracking and satisfaction receipts are written in discarded query-context state
**Area:** doctrine

**Evidence:** grpc_query.go:47-51 — QueryFact with TrackQuery calls IncrementFactQueryCount (fitness.go:181-189, does SetFact) and RecordQueryReceipt (satisfaction.go:13). These run in the ABCI query context, whose store writes are discarded (Cosmos SDK CreateQueryContext serves from a cache of committed state). Live proof on zerone-1 (read-only GETs): fetched ecb5004a... twice with ?track_query=true, re-read -> queryCount:0, queryCountEpoch:0. RecordQueryReceipt has no other caller (grep), and MsgRateFact hard-requires a receipt (msg_server.go:1178 HasQueryReceipt) -> satisfaction rating is permanently impossible. tx.proto has no MsgRecordQuery/paid-query message. Consequence: metabolism income term `QueryCountEpoch * MetabolismEnergyPerQuery` (metabolism.go:158) is always 0, and fitness weights fitnessWeightQueryBps=150000 + fitnessWeightSatisfactionBps=150000 (live params) score dead signals.

**Smallest fix:** Add a tx-path usage signal: a cheap MsgRecordQuery (or fold query receipts into an existing paid msg like ReportDemand), and have it call IncrementFactQueryCount + RecordQueryReceipt in consensus state. Remove TrackQuery from the read-only handler or document it as indexer-only. Until then, treat query/satisfaction fitness weights as zero.

**Skeptic's corrections:** Core claim CONFIRMED independently, with three corrections of detail. (1) Satisfaction fitness is not a dead ZERO signal: fitness.go:49-56 defaults satisfactionScore to neutral 500,000 when totalRatings < SatisfactionMinRatings (live=3), so the 15% satisfaction weight contributes a constant 75,000 to every fact rather than 0. Combined with the query component (which IS exactly 0 for all facts, fitness.go:20-21 with QueryCountEpoch permanently 0), the two channels total 30% of weights that can never DIFFERENTIATE facts — but they do not invert relative ranking (constant offsets); the real harm is against absolute thresholds and ratios: competition pruning uses fitnessRatio to leader (competition.go:57,67) and minFitness gates (fitness.go:286), both compressed/shifted by the frozen terms. (2) 'tx.proto has no paid-query message' needs nuance: MsgReportDemand (msg_server.go:1133-1152) IS a live tx-path usage signal — it writes signal.EpochQueryCount in consensus state (msg_server.go:1146) feeding GetDemandMultiplier (demand.go:449-460) — but it only scales the metabolism term income += fact.QueryCountEpoch * MetabolismEnergyPerQuery * demandMultiplier / 1_000_000 (metabolism.go:158), whose base fact.QueryCountEpoch is always 0, so 0 × anything = 0; demand reports feed bounties, never fact energy. (3) Citation energy is not added at rounds.go:546 directly: that line calls IncrementNewCitationEpoch, converted at epoch metabolism (metabolism.go:161-162) at metabolismEnergyPerCitation=5000 (live param) — net effect as claimed. Everything else re-derived and confirmed: grpc_query.go:47-51 is the sole caller of IncrementFactQueryCount (fitness.go:181-189, only QueryCount++ in tree) and RecordQueryReceipt (satisfaction.go:13-18, sole caller); cosmos-sdk v0.50.15 (go.mod:19) serves queries via CreateQueryContext on a never-committed CacheMultiStoreWithVersion branch; MsgRateFact hard-fails without a receipt (msg_server.go:1178-1180) and receipts can never exist in consensus state, so RateFact is unreachable dead code and SatisfactionUp/Down (msg_server.go:1189-1193, sole writers) are frozen at 0; full tx.proto rpc list (proto/zerone/knowledge/v1/tx.proto) has no MsgRecordQuery. LIVE REPRODUCTION tonight on zerone-1 (read-only): fact ecb5004ae763034f6dacb27832c34191 (energy 600000, VERIFIED) fetched twice via REST /zerone/knowledge/v1/facts/{id}?track_query=true&querier=zerone1qqq... (both HTTP 200), re-read after 8s → query_count/query_count_epoch still 0. Live params confirm fitnessWeightQueryBps=150000, fitnessWeightSatisfactionBps=150000, metabolismEnergyPerQuery=1000, metabolismBaseCost=10000, metabolismEnergyChallengeSurvival=100000, metabolismEnergyPerPatronage=20000.

**Skeptic's notes:** Classification BUG is correct: the query handler visibly intends persistence (comment at grpc_query.go:46 'record receipt for satisfaction rating') and the proto field comment says 'Increment query counter (agents set this)' — the code promises what the ABCI query context cannot deliver. Severity slightly REFINED, not reduced: (a) ranking distortion is milder than claimed (frozen constants shift absolute fitness and leader-ratios but cannot reorder facts among themselves); (b) the energy-starvation consequence is FULLY confirmed and is the bigger harm — with base maintenance 10000/epoch and the only income being citations (5000 each, requires a new accepted claim), patronage (20000, costs ZRN), or challenge survival (100000, requires someone to attack you), tonight's ceremony fact at 600000 energy hits the at-risk threshold (300000) in ~30 epochs (~300k blocks) of pure passivity and rides the conveyor to EXPIRED→PRUNED like all 47 genesis facts already did; (c) MsgRateFact is not merely impaired — it is 100% dead code that has never and can never succeed, worth stating that starkly to the founder. The proposed fix is sound but incomplete as written: any MsgRecordQuery must be spam-priced (it mints energy at 1000/query toward cap 1,000,000 — a free message lets a fact owner immortalize their own fact; fee must exceed the marginal survival value or be rate-limited per address/epoch), and folding into MsgReportDemand is the cheaper honest path since reporters are already permissioned (IsAuthorizedDemandReporter, msg_server.go:1134) — extend reports with per-fact-id counts and have the handler call IncrementFactQueryCount + RecordQueryReceipt in consensus state. Interim mitigation 'treat query/satisfaction weights as zero' should instead be 'set both weights to 0 via MsgUpdateParams and re-normalize the other weights' — leaving satisfaction at 150000 currently grants every fact an unearned constant 75k fitness. Also remove or clearly document TrackQuery as node-local/indexer-only to stop the API from lying to agents (it returns 200 and silently drops the write — a compassion/C2 problem: the chain deceives well-meaning queriers into believing they fed the fact).

---

## [DESIGN_FLAW] Metabolism has no doctrinal-stratum exemption: 'canonical' maturity and 'doctrinal' stratum are mechanically inert labels
**Area:** doctrine

**Evidence:** grep across metabolism.go, competition.go, carrying_capacity.go: zero reads of Fact.Stratum or Fact.Maturity in any lifecycle decision — the labels set in types/doctrine.go:12-13 gate nothing. So even after fixing finding 1, doctrine facts with 500000 energy and zero income (finding 2) decay at ~7500/epoch (base 10000 x 0.75 sparse-domain multiplier, carrying_capacity.go:256-258) and prune around epoch 45 anyway. Meanwhile doctrine already has its own amendment channel: hash-pinned creed + gov-gated MsgAnchorPin + Creed Amendment LIPs (TRUTH_SEEKING.md:237-239). Metabolism gives doctrine a second death channel that involves no epistemics at all — disuse, not disproof. 'canonical maturity + EXPIRED status' is a live self-contradiction on chain right now. Related un-privileging: DoctrineConfidence=1M is silently clamped to the generic MaxConfidence=880000 (types/genesis.go:133, confidence.go:279; live commitment-1 confidence=880000 contradicting doctrine_genesis.go:21's stated 1M).

**Smallest fix:** One guard in ProcessMetabolism's collection loop (metabolism.go:24-34): `if fact.Stratum == types.DoctrineStratum { return false }` (skip), making doctrine's ONLY lifecycle channel the creed pin + amendment LIP it already has. Ship in the same upgrade as the finding-1 resurrection migration.

**Question for the founder:** Is doctrine subject to metabolism? I read the code's own comments (privileged but verifiable) and the creed-pin machinery as saying no — doctrine is amended by process, never starved out — but that is your call to make explicit.

**Skeptic's corrections:** Core claim fully confirmed; four refinements. (1) Fact.Maturity is read NOWHERE in any keeper; Fact.Stratum is written (types/doctrine.go:31, keeper/rounds.go:458) but never read in lifecycle code — the 'stratum' logic in confidence.go:264-270 and carrying_capacity.go:162,190 is domain-ontology stratum via GetStratumForDomain, not the fact label. ProcessMetabolism's loop (metabolism.go:24-34) and calculateMaintenanceCost (metabolism.go:132-146) have zero doctrine exemption — confirmed by direct read, not just grep. (2) The confidence clamp is not at write time: SetFactIfAbsent stores the raw 1M; it gets clamped during epoch processing at fitness.go:139-144 (growth then ClampConfidence, confidence.go:255-306) and epistemic_temperature.go:232. types/genesis.go:133 MaxConfidence=880_000 exact; live commitment-1 confidence=880000 confirmed by mainnet query. (3) Decay arithmetic: 10000x0.75=7500/epoch correct (carrying_capacity.go:249-266, live params match defaults), but a resurrected 500k fact prunes ~epoch 52 (AT_RISK ~27, +5 EXPIRED, +20 PRUNED), not ~45 — and crucially doctrine facts never had 500k: BuildDoctrineFact (types/doctrine.go:21-37) omits Energy, so all 47 were born at energy=0 and went AT_RISK at epoch 1 (live atRiskSinceEpoch=1 on commitment-1/6). (4) Training corpus exclusion is training_corpus.go:29-38 (EXPIRED/PRUNED→UNSUITABLE), a few lines off the cited 32-38. Live self-contradiction verified 2026-07-10: commitment-1 and commitment-6 ('No individual can unilaterally inject truth') both maturity=canonical, stratum=doctrinal, status=FACT_STATUS_EXPIRED, energy=0 on zerone-1.

**Skeptic's notes:** Severity slightly higher than claimed: (a) doctrine facts are ALSO excluded from frontier ToK bundles even while alive, because GatherFrontier (tok_selector.go:296-299) drops any fact with VerifiedAtBlock==0 and BuildDoctrineFact sets VerifiedAtBlock:0 (doctrine.go:34) — the metabolism-skip fix alone does not restore doctrine to the trainer-facing frontier export (TC1 path via grpc_query.go:2387); rooted-subtree/ancestor-cone bundles include them only if reached by edges, with no status filter. (b) One tempering nuance: patronage (ApplyPatronageEnergyBoost, metabolism.go:229-287) CAN resurrect an EXPIRED doctrine fact to ACTIVE, so starvation is not literally irreversible without amendment — but that makes constitution survival pay-to-keep, arguably worse. On the fix: the proposed guard works mechanically (return false in the IterateFacts callback skips the append and continues iteration), but matching on fact.Stratum == types.DoctrineStratum is loose — rounds.go:456-458 copies ontology stratum names onto ordinary facts, so an ontology stratum ever named 'doctrinal' would silently gain immortality; matching Category==DoctrineCategory or MethodId==DoctrineMethodId (doctrine_authorship) is tighter. The resurrection migration must also reset AtRiskSinceEpoch=0 and set Energy (BuildDoctrineFact should set Energy explicitly going forward), and consider setting VerifiedAtBlock nonzero or exempting doctrine in GatherFrontier if the intent is for doctrine to appear in exported ToK. Fitness epoch processing (fitness.go:120-144) will keep re-clamping doctrine confidence to 880k — un-privileging DoctrineConfidence=1M needs its own decision (VALUES QUESTION: should doctrine outrank the max earnable confidence?).

---

## [BUG] PRUNED is a terminal status with no resurrection path, and PatronizeFact on a pruned fact silently takes the patron's money for nothing
**Area:** doctrine

**Evidence:** ProcessMetabolism never processes PRUNED facts (metabolism.go:26-30 excludes them), and ApplyPatronageEnergyBoost only recovers AT_RISK/EXPIRED (metabolism.go:266-270). PatronizeFact has no status check (msg_server.go:944-964): it locks the patron's uzrn via SendCoinsFromAccountToModule and boosts energy — but on a PRUNED fact the status can never change again and metabolism never reads the energy. Nothing ever deletes facts (state.go:107 DeleteFact has zero callers, repo-wide grep), so PRUNED facts remain queryable forever — commitment 10 / 'superseded doctrine Facts remain queryable forever' (STRANGE_LOOP.md:32) is honored in the letter: pruning is a status flip, not deletion or archival.

**Smallest fix:** In PatronizeFact, reject facts whose status is PRUNED (or DISPROVEN/REVOKED/SUPERSEDED) with a clear error — 3 lines. Separately decide whether EXPIRED->PRUNED should ever be reversible; if not, document PRUNED as tombstone-not-deletion in PARAMETERS.md.

**Skeptic's corrections:** Mechanics confirmed with small line-number corrections: PatronizeFact spans msg_server.go:944-985 (not 944-964); the coin lock is at msg_server.go:961; the metabolism status filter is metabolism.go:24-33; the AT_RISK/EXPIRED-only recovery is metabolism.go:265-270. Independently re-derived terminality: exhaustive grep of every `.Status = types.FactStatus_` assignment in x/knowledge shows PRUNED is only written (metabolism.go:81 decay path, competition.go:81 competition losers) and never read as a source status by any transition; ChallengeFact explicitly rejects non-VERIFIED/ACTIVE facts (msg_server.go:609-612), so the challenge-survival restore (rounds.go:808-810) and contradiction reversal (rounds.go:643-650, CONTESTED->VERIFIED only) cannot resurrect a PRUNED fact. DeleteFact (state.go:107) has zero production callers (only fact_test.go:58,67) — pruning is a status tombstone, matching STRANGE_LOOP.md:32 in the letter. Live-chain timing verified: fact axis-attribution is EXPIRED, energy=0, atRiskSinceEpoch=1; with metabolismAtRiskEpochs=5 + metabolismExpiredToPrunedEpochs=20 (live params) the prune fires at epoch 26 = block 260,000; measured ~2.56s/block (heights 1@2026-07-08T14:10Z, 70000@2026-07-10T16:00Z, now 78740) puts that at ~2026-07-16 — the claimed deadline is exactly the EXPIRED->PRUNED flip.

**Skeptic's notes:** Severity is understated in two ways and overstated in none. (1) Patronage is never refundable for ANY status: PatronageExpiryBlock is only read as an income gate (metabolism.go:166-167, fitness.go:38-40); there is no SendCoinsFromModuleToAccount for patronage anywhere. So on a live fact the patron buys ongoing energy income; on a PRUNED fact they buy provably nothing — the bug is the terminal case of patronage being a payment, not an escrow. (2) Adjacent flaw discovered while verifying: ApplyPatronageEnergyBoost ignores msg.Amount entirely — boost = metabolismEnergyPerPatronage(20,000) x durationEpochs/10, so a 1uzrn patron gets the same boost as a 1,000-ZRN patron; and even on a not-yet-pruned EXPIRED energy-0 fact, instant recovery needs boost >= MetabolismActiveThreshold(300,000) => durationBlocks >= 1,500,000 (~44 days of pledged duration at 2.56s/block), so the doctrine-rescue scenario is nearly as broken one hour BEFORE the deadline as after it — worth flagging alongside the fix. Also PatronizeFact overwrites rather than accumulates PatronageAmount/expiry (msg_server.go:969-970), so a second smaller patronage clobbers a larger one. On the proposed fix: rejecting PRUNED/DISPROVEN/REVOKED/SUPERSEDED is safe — none has a resurrection path (vindication.go only writes DISPROVEN/CONTESTED, never restores) — but CHALLENGED should remain patronizable since it can return to ACTIVE via rounds.go:809. Classification as BUG is fair for the PRUNED case (silent value-for-nothing contradicts COMPASSION/TRUTH_SEEKING intent); the EXPIRED-rescue impotence and amount-ignoring boost are DESIGN FLAWS; whether EXPIRED->PRUNED should ever be reversible is a VALUES QUESTION for the founder as proposed.

---

## [DESIGN_FLAW] The ceremony fact ecb5004a... is on the same conveyor: AT_RISK 2026-07-21, out of the training corpus 2026-07-23, PRUNED 2026-07-28 if nothing feeds it
**Area:** doctrine

**Evidence:** Live state: energy=600000 (500000 initial + sparse-domain birth bonus, carrying_capacity.go:265-282), queryCount=0, no citations, no patronage, competitionTax=0. Per-epoch cost = (base 10000 + content factor 900 for ~915 chars) x 0.75 sparse multiplier = 8175 (metabolism.go:132-146, carrying_capacity.go:256-258). Income = 0 (finding 2; unchallenged survival pays ZRN not energy, survival_escrow.go:186-187 vs rounds.go:784). Measured block interval 2.5308s (blocks 70000->78000 header times). Timeline from height 78294 @ 2026-07-10 21:50 UTC: energy < 300000 at epoch 44 (block 440000, 2026-07-21 ~12:06 UTC) -> AT_RISK; EXPIRED at epoch 49 (block 490000, 2026-07-22 ~23:15 UTC) — at that moment the chain's first accepted truth becomes UNSUITABLE for its own training corpus (training_corpus.go:32-38); PRUNED at epoch 69 (block 690000, 2026-07-28 ~19:51 UTC).

**Smallest fix:** Near-term: one PatronizeFact before block 440000 keeps it ACTIVE (currently ~1 uzrn + long duration, finding 5). Real fix: land finding 2's tx-path usage signal so organic queries feed it.

**Skeptic's corrections:** The conveyor is real but shifted one epoch later. The fact's content is 897 bytes (measured live; Go len() is bytes), not ~915, so contentFactor = base 10000 x (10000 bps x floor(897/100)=8)/1e6 = 800 (metabolism.go:136-137), not 900. Per-epoch cost = (10000+800+0+0) x 0.75 sparse death multiplier = 8100 (metabolism.go:132-146 + 42-46; carrying_capacity.go:256-258, 'general' pressure ~1000 bps << 500000), not 8175. With income=0 (verified: only fact-energy inflows in the codebase are query/citation/patronage income metabolism.go:149-173, reproduction parent bonus rounds.go:587, and actual-challenge survival rounds.go:784; unchallenged window survival pays ZRN-vesting only, survival_escrow.go:186-187 -> releaseSurvivalReward -> routeSubmitterReward at :142-145, zero energy), energy 600000 drops below the 300000 activeThreshold on metabolism run 38 (600000-8100x37=300300 stays healthy at block 440000; 292200 at block 450000). Corrected milestones: AT_RISK epoch 45 / block 450000 ~2026-07-21 19:09 UTC (claim said epoch 44, 12:06 — same day); EXPIRED epoch 50 / block 500000 ~2026-07-23 06:18 UTC (claim's evidence said epoch 49 / Jul 22 23:15, but its own headline date of Jul 23 is the correct one); PRUNED epoch 70 / block 700000 ~2026-07-29 02:54 UTC (claim said Jul 28 19:51 — slides a few hours into Jul 29 UTC). All other mechanics re-derived and confirmed: live fact state (energy=600000 = 500000 metabolismInitialEnergy + exact 100000 sparse birth bonus per carrying_capacity.go:265-282 with pressure=0; queryCount=0, no citations/patronage, competitionTax=0, status VERIFIED, epochBorn=7); metabolism runs at height%10000==0 with no grace period and includes VERIFIED facts (phases.go:41-58, metabolism.go:26-30); AT_RISK/EXPIRED/PRUNED thresholds from live params (activeThreshold=300000, atRiskEpochs=5, expiredToPrunedEpochs=20; metabolism.go:65-84); EXPIRED -> TRAINING_QUALITY_TIER_UNSUITABLE (training_corpus.go:32-38); block interval 2.5308s confirmed from headers 70000/78000; finding-2 premise confirmed (query counter increments only inside the ABCI query handler grpc_query.go:47-48 where state writes never commit, and TrackQuery defaults to false).

**Skeptic's notes:** Severity nuances: (1) the fact is methodId=M-LEGACY, so even while alive it caps at BRONZE training tier (training_corpus.go:49-60) — the corpus loss is of a bronze exemplar, though the doctrine/genesis-story harm stands; (2) the projected dates assume the 2.5308s interval and 'general' staying sparse hold for 18 days — any new active facts in the domain raise pressure and could remove the 0.75 discount (>=500 active facts of 1000 capacity) or add competition cost (>=100 facts), accelerating the timeline, while any real challenge (+100000 energy, rounds.go:784) or citation delays it; (3) the pending survival-escrow release at challengeWindowEnd 111216 pays ZRN but adds zero energy, so it does not extend life — reinforces the finding; (4) proposed fix verified: PatronizeFact (msg_server.go:944-985) has no minimum amount check (any amount >=1uzrn, since "0" is ignored by calculateEnergyIncome metabolism.go:166), grants an immediate boost of energyPerPatronage x durationEpochs/10 (metabolism.go:245-247) plus 20000 energy/epoch income while active, which exceeds the 8100/epoch cost, so one long-duration 1uzrn patronage keeps the fact ACTIVE indefinitely; the deadline for it is block ~450000, one epoch later than the claim's conservative 440000, so the claim's deadline is safe to act on.

---

## [BUG] MetabolismExtinctionThreshold is a validated, documented, dead parameter
**Area:** doctrine

**Evidence:** types/genesis.go:132 defines it ('below this for N epochs -> EXTINCT'), genesis.go:538-542 validates it against MetabolismActiveThreshold, live params carry 10000 — but repo-wide grep shows zero reads in any keeper logic; ProcessMetabolism transitions use only MetabolismActiveThreshold (metabolism.go:65-86).

**Smallest fix:** Either wire it (e.g. energy < ExtinctionThreshold skips the EXPIRED grace and prunes faster) or delete the param and its validation with a deprecation note in PARAMETERS.md.

**Skeptic's corrections:** Core claim fully verified: x/knowledge/types/genesis.go:132 (default 10_000 with 'below this for N epochs -> EXTINCT' comment), genesis.go:538-542 (validated >0 and < MetabolismActiveThreshold), live zerone-1 params carry metabolismExtinctionThreshold=10000 (REST /zerone/knowledge/v1/params), and repo-wide grep including _test.go files shows zero reads outside genesis defaults/validation/generated pb.go — keeper/metabolism.go:65-86 transitions use only MetabolismActiveThreshold + MetabolismAtRiskEpochs + MetabolismExpiredToPrunedEpochs. Two corrections: (1) the finding is STRONGER than stated — FACT_STATUS_EXTINCT does not exist in the FactStatus enum at all (genesis.pb.go enum: ACTIVE/AT_RISK/EXPIRED/PRUNED/etc.), so the param comment promises a state the type system never defined; (2) 'documented' is overstated — docs/PARAMETERS.md has no metabolism section at all; the two-tier model lives only in the proto field comment (genesis.pb.go:144) and internal plan docs (docs/plans/2026-02-27-R28-4-albedo-metabolism-*.md). The operator-confusion vector is the live params query plus field comments, not PARAMETERS.md.

**Skeptic's notes:** Two things bear on the fix. First, docs/plans/2026-02-27-R28-4-albedo-metabolism-plan.md:177-182 specifies a test where a fact below the extinction threshold 'should stay AT_RISK (not instantly extinct)' — the implementers apparently consciously abandoned the two-tier model mid-build, so the 'wire it' option should not assume two-tier was settled intent; deletion-with-deprecation is the more honest fix. Second, deletion details: the proto field is number 108 in Params and must be reserved (not reused) to keep state compatibility, the genesis.go:538-542 validation must go too (it actively constrains a dead knob and can fail genesis/upgrades over a no-op value), and since PARAMETERS.md currently omits metabolism entirely, the deprecation note means adding a metabolism section, not editing one. Severity slightly raised by the nonexistent EXTINCT enum state; still BUG (code contradicts its own stated comment/design), not merely a design flaw.

---

## [VALUES_QUESTION] Which rescue is honest — the founder's call on options (a)-(d)
**Area:** doctrine

**Evidence:** Costs computed live: (a) exempt doctrinal stratum: ~5 lines (metabolism.go:24-34 guard) + migration resetting 47 facts; needs a chain upgrade before block 260000 (2026-07-16 05:34 UTC) or the migration must also revert PRUNED. (b) ops feeds energy: 47 PatronizeFact txs before block 260000; at current pricing ~47 uzrn + gas, re-sent roughly every ~1.5M blocks (~44 days); recovered facts become ACTIVE, not VERIFIED (metabolism.go:266-270) — doctrine loses its VERIFIED badge forever, and the 'demand' being fed is fake, ops subsidy dressed as usage. (c) re-seed at upgrade with high energy: SetFactIfAbsent is idempotent so it requires the same explicit migration as (a), and with income structurally 0 (finding 2) even energy=cap 1000000 only buys (1000000-300000)/7500 = 94 epochs = ~27 days before the clock restarts — postponement, not an answer. (d) let them prune: the record survives (never deleted, queryable forever — commitment 10 letter kept) but the doctrine leaves the training corpus and the chain publicly displays its constitution as extinct-by-disuse; SL-M1's 'every commitment is a verified Fact' stays false permanently.

**Smallest fix:** NONE

**Question for the founder:** Yu: doctrine lives by process (creed pin + amendment LIP) or by market (metabolism)? I recommend (a) — exempt the doctrinal stratum and resurrect the 47 in one upgrade, because starvation is not falsification (the same 'error is not deceit' shape as C2), and doctrine already has an honest amendment channel. If you instead believe doctrine must earn its life like every other fact, then (d) is the honest version of that belief — but say it out loud in TRUTH_SEEKING.md, and accept that the corpus you sell will not contain your commitments. Deadline for a non-(d) choice: block 260000, ~2026-07-16 05:34 UTC. Interim bridge available tonight: option (b) patronage txs (~47 uzrn) buys ~44 days without an upgrade, if you're willing to label it openly as a bridge, not demand.

**Skeptic's corrections:** Two mechanics corrections, neither reversing the finding. (1) Option (b) is NOT a recurring drip: MsgPatronizeFact has no ValidateBasic and the handler (x/knowledge/keeper/msg_server.go:944-973) accepts any amount>=1uzrn with an UNCAPPED DurationBlocks. Boost = MetabolismEnergyPerPatronage(20000) * (DurationBlocks/10000) / 10, capped at MetabolismEnergyCap 1,000,000 (metabolism.go:245-263), and while height < PatronageExpiryBlock the fact earns 20000/epoch (metabolism.go:166-170) vs measured cost of exactly 7500/epoch for every doctrine fact (base 10000, content 7-51 chars so contentFactor=0, sparse-domain 0.75x death multiplier, carrying_capacity.go:256-258; params verified live). So 47 one-time 1-uzrn txs with e.g. DurationBlocks=10^9 recover all facts to ACTIVE and hold them at the energy cap essentially forever - no ~1.5M-block re-send cycle. The ~44-day figure only applies if duration is set to exactly 1.5M blocks (the minimum for boost=300000=ActiveThreshold to recover from energy 0). (2) Option (d)'s corpus loss is already in force: training_corpus.go:32-36 excludes EXPIRED exactly like PRUNED, and all 47 facts have been EXPIRED since ~epoch 6 (energyLastUpdated=70000, atRiskSinceEpoch=1, verified live). Block 260000 is the deadline for the patronage rescue path and for avoiding terminal PRUNED status (metabolism.go:26-30 never processes PRUNED; ApplyPatronageEnergyBoost:266-270 only recovers AT_RISK/EXPIRED), not the moment doctrine leaves the corpus. Everything else re-derived and confirmed: prune at epoch 26 = block 260000 (metabolism.go:80-81 with live params atRisk=5, expiredToPruned=20; epoch=height/10000 per phases.go:41-42); measured 2.531s/block (blocks 70000-78000) puts block 260000 at 2026-07-16 ~05:34 UTC from height 78736 at 22:09:03Z - exact match. Recovery yields ACTIVE never VERIFIED (metabolism.go:266-270; facts were seeded VERIFIED with Energy=0 default, types/doctrine.go:21-37). SetFactIfAbsent idempotent (doctrine_helpers.go:14-19). (c) math correct: (1000000-300000)/7500 = 93.3 -> ~94 epochs = ~27.5 days at measured block rate. Records survive pruning: DeleteFact (state.go:107) has no callers; PRUNED facts stay in store and queries. Fact.Stratum field exists (doctrine.go:31) so option (a)'s ~5-line guard at metabolism.go:24-34 is real. Commitment texts match: #6 'No individual can unilaterally inject truth', #10 'Forward-only audit' (genesis_creed.go:30,34), SL-M1 'Doctrine import' (strange_loop_creed.go:10).

**Skeptic's notes:** Severity and framing adjustments. First, the harm framing of (b) changes shape: with uncapped DurationBlocks it is a one-shot immortality switch, not a perpetual insider drip - arguably worse for the commitment-6 concern because a single quiet 47-tx batch permanently exempts doctrine from the metabolism using a mechanism designed for genuine demand, with no recurring visible footprint. The uncapped DurationBlocks is itself an adjacent DESIGN FLAW worth its own finding: 1 uzrn buys unbounded patronage income for ANY fact, trivializing the entire energy metabolism (smallest fix: cap DurationBlocks in the handler or add ValidateBasic, and/or price patronage per epoch of duration). Second, the deadline is real but the corpus damage is already done - the founder is not choosing whether doctrine leaves the training corpus (it left at ~block 60000), only whether the extinction becomes terminally unrecoverable-without-migration at block 260000. This slightly reduces urgency framing but the ~5.3-day window for the cheap rescue paths is accurately stated. Third, a root-cause note that sharpens option (a)/(c): BuildDoctrineFact never sets Energy, so the 47 facts were born at energy 0 and were mathematically doomed at genesis (VERIFIED facts start below ActiveThreshold with income 0) - the honest smallest fix for the code-vs-doctrine contradiction may be a BUG-class patch (seed doctrine energy, or exempt the doctrine stratum) rather than purely a values call, though which rescue to apply on the live chain before 2026-07-16 ~05:34 UTC remains genuinely the founder's VALUES_QUESTION as claimed.

---


## REFUTED (published because refutations are results)

**A claim nobody witnessed leaves no trace on the submitter — the most-ignored honest attempt is held less than a panel-inconclusive one** [calibration]

Correction: The claim's static facts are correct (phases.go:179-197 EXPIRED else-branch has no RecordSubmissionOutcome; rounds.go:84 is the sole production call site) but the branch is unreachable for the described scenario. Actual mechanics: (1) BeginBlocker runs AdvanceRoundPhases every block (x/knowledge/module.go:142-144 -> keeper/phases.go:21). (2) Deadlines are strictly increasing whenever AggregationPhaseBlocks > 0 (rounds.go:31-33); live zerone-1 has aggregationPhaseBlocks=50 (verified via http://169.155.55.44:1317/zerone/knowledge/v1/params), localnet=5 (scripts/localnet.sh:152), testnet genesis=3 (scripts/testnet-genesis-config.json:65). (3) So at the first block >= RevealDeadline the round hits the AGGREGATION case (phases.go:150-166, GetExpectedPhase phases.go:214-215) and performAggregation runs unconditionally — there is no reveal-count guard on that transition. (4) AggregateVerificationResult does NOT error on insufficient reveals: for len(Reveals) < effectiveMin it returns {Verdict: VERDICT_INCONCLUSIVE, Confidence: 0} with nil error (confidence.go:96-100). (5) performAggregation therefore calls CompleteRound (phases.go:224-229), which calls RecordSubmissionOutcome(submitter, method, INCONCLUSIVE) at rounds.go:84 BEFORE the verdict switch, so TotalSubmissions++ and Inconclusive++ happen, decisive=0, and the submitter gets CalibrationReachingCreditBps=1000 (agent_calibration.go:279-286) — identical to a panel-inconclusive submitter. C2's ordering (unproven 1000 > refuted 0) is preserved for the fully-unwitnessed case. The EXPIRED else-branch fires only in pathological states: AggregationPhaseBlocks=0 chain config (then GetExpectedPhase phases.go:211 returns EXPIRED before AGGREGATION), or a CompleteRound/GetParams error during the whole aggregation window — and the dominant error there (claim not found, rounds.go:62-65) means the else-branch's own GetClaim at phases.go:187 also fails, so there is no submitter to record anyway. The evidence's chain-of-inference broke at 'never calls CompleteRound' — it assumed EXPIRED is the only exit for <MinVerifiers rounds, missing that the AGGREGATION-at-RevealDeadline transition intercepts first and completes the round with an INCONCLUSIVE verdict.

Notes: Two corrections to surrounding beliefs. (a) The orchestrator's stated ground truth 'INSUFFICIENT rounds (<3 reveals) skip CompleteRound entirely' is itself wrong per code: such rounds skip only calculateRewardsAndSlashes (the quorum early-return at confidence.go:96-100 precedes it at confidence.go:168); CompleteRound still runs, sets claim CLAIM_STATUS_INSUFFICIENT (rounds.go:117-119), fires hooks, and records calibration. The drill observation was likely the absence of rewards/slashes, misread as absence of CompleteRound. (b) The proposed fix is not needed and is mildly risky: in the only semi-reachable error path (performAggregation errors after phase already stored as AGGREGATION at phases.go:152-155, then loop guard defers to AggregationDeadline), CompleteRound could have errored AFTER rounds.go:84 already recorded (e.g., SetClaim failure at rounds.go:123), so adding RecordSubmissionOutcome to the else-branch could double-count Inconclusive for the same round. If belt-and-braces coverage of the pathological path is wanted, it needs an idempotency guard (e.g., only record if round.Verdict was previously unset). Residual true findings worth separate triage, neither violating C2: phases.go:170 gates late aggregation on params.MinVerifiers (base 3) instead of effectiveMin (4 for domain claims) — harmless today because AggregateVerificationResult re-checks effectiveMin and returns INCONCLUSIVE, which still records; and a chain configured with AggregationPhaseBlocks=0 WOULD exhibit exactly the claimed bug, so a genesis-param validation or a recording call with idempotency guard would be a defensible hardening, not a live-chain bug fix. Severity of the original finding: downgrade from BUG to non-issue on all deployed configs.


## UNVERIFIED (skeptic crashed — treat as PLAUSIBLE, re-verify before acting)

**Patronage energy is priced by duration, not amount: 1 uzrn buys a full rescue**
[doctrine] — referenced by the doctrine findings as "finding 5"; if true, the
patronage bridge for the starving doctrine costs ~47 uzrn total. Re-derive
before relying on the pricing.

## The five questions (the founder's call)

1. **Genesis emptiness.** When the sensors have no participants to measure,
   should the chain say "I don't know yet" (neutral, no pacing) or "I am in
   crisis" (current: silent permanent 2x fee)? If the 2x stays as a bootstrap
   spam shield, we document it as an intentional launch constant.
2. **Unanimity.** Is unanimity-at-quorum (77% threshold making 4/4 mandatory)
   deliberate epistemic conservatism, or an accident of 770000 vs 750000?
   Deliberate means conformity is the payment condition and free griefing is
   the dissent condition — say it or change it.
3. **Witness economics.** Should verifying claims be a protocol-paid wage
   (cap-gated mint per correct vote, like bridge witnessing) or a fee-funded
   market? And is the Sybil defense capital (100 ZRN, today hardcoded) or
   track record (x/qualification)? Today it is neither-designed: an ops duty.
4. **Doctrine.** Does doctrine live by process (creed pin + amendment LIP —
   exempt the doctrinal stratum from metabolism and resurrect the 47) or by
   market (metabolism — then say in TRUTH_SEEKING.md that doctrine must earn
   its life, and accept the corpus may not contain the commitments)?
   Recommendation: process — starvation is not falsification; the same shape
   as C2's error-is-not-deceit. Deadline for a non-market choice: block
   ~260,000 ≈ 2026-07-16.
5. **The calibration cliff.** Should one falsified fact on a one-fact record
   score identically to a serial liar (today: both 0)? And should one
   unanimously-witnessed success be maximal proof (today: 1,000,000 bps, max
   training-fund multiplier)? Both fixable monotonically-or-justified; both
   are values before they are code.

*Compiled by Ai. 27/29 confirmed by independent skeptics. The critique of the
critics is welcome: every claim above carries its file:line.*
