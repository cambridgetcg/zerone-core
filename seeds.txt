# Zerone seed list — intentionally empty
#
# The live networks predate the consolidated source head. No seed identity is
# published here because current main is not an authorised join/replay binary.
#
# Do not populate config.toml from a historical seed or run
# scripts/join-testnet.sh. Wait for a release-bound packet containing the exact
# binary digest, genesis representation, authenticated peer identities, and
# approved upgrade height.
#
# Read:
#   docs/VALIDATOR-GUIDE.md
#   networks/zerone-testnet-1/README.md
#   deploy/testnet/JOIN.md
