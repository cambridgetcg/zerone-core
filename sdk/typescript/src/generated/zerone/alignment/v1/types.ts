//@ts-nocheck
import { BinaryReader, BinaryWriter } from "../../../binary";
import { DeepPartial } from "../../../helpers";
/**
 * AlignmentState tracks the module's operational state.
 * @name AlignmentState
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.AlignmentState
 */
export interface AlignmentState {
  enabled: boolean;
  lastObservationHeight: bigint;
  observationCount: bigint;
  /**
   * Whether degraded-frequency mode is active (2x observation rate).
   */
  degradedFrequencyActive: boolean;
  /**
   * Previous health category for transition detection.
   */
  previousCategory: string;
}
/**
 * AlignmentObservation records sensor readings at a given height.
 * @name AlignmentObservation
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.AlignmentObservation
 */
export interface AlignmentObservation {
  height: bigint;
  timestamp: bigint;
  /**
   * Dimension scores in BPS (0–1,000,000 = 0–100%).
   */
  knowledgeQuality: bigint;
  economicStability: bigint;
  governanceParticipation: bigint;
  networkSecurity: bigint;
  stakingRatio: bigint;
}
/**
 * DimensionScores holds weighted and composite scores for a height.
 * @name DimensionScores
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.DimensionScores
 */
export interface DimensionScores {
  height: bigint;
  /**
   * Raw dimension scores (BPS).
   */
  knowledgeQuality: bigint;
  economicStability: bigint;
  governanceParticipation: bigint;
  networkSecurity: bigint;
  stakingRatio: bigint;
  /**
   * Weighted composite score (BPS).
   */
  composite: bigint;
}
/**
 * CorrectionRecord logs a correction generated by alignment analysis.
 * @name CorrectionRecord
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.CorrectionRecord
 */
export interface CorrectionRecord {
  height: bigint;
  dimension: string;
  parameter: string;
  direction: string;
  magnitude: bigint;
  applied: boolean;
  timestamp: bigint;
}
/**
 * AlignmentHealthIndex is the composite health metric for a given height.
 * @name AlignmentHealthIndex
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.AlignmentHealthIndex
 */
export interface AlignmentHealthIndex {
  height: bigint;
  compositeScore: bigint;
  category: string;
  dimensionalScores?: DimensionScores;
  correctionsGenerated: number;
}
function createBaseAlignmentState(): AlignmentState {
  return {
    enabled: false,
    lastObservationHeight: BigInt(0),
    observationCount: BigInt(0),
    degradedFrequencyActive: false,
    previousCategory: ""
  };
}
/**
 * AlignmentState tracks the module's operational state.
 * @name AlignmentState
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.AlignmentState
 */
export const AlignmentState = {
  typeUrl: "/zerone.alignment.v1.AlignmentState",
  encode(message: AlignmentState, writer: BinaryWriter = BinaryWriter.create()): BinaryWriter {
    if (message.enabled === true) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.lastObservationHeight !== BigInt(0)) {
      writer.uint32(16).uint64(message.lastObservationHeight);
    }
    if (message.observationCount !== BigInt(0)) {
      writer.uint32(24).uint64(message.observationCount);
    }
    if (message.degradedFrequencyActive === true) {
      writer.uint32(32).bool(message.degradedFrequencyActive);
    }
    if (message.previousCategory !== "") {
      writer.uint32(42).string(message.previousCategory);
    }
    return writer;
  },
  decode(input: BinaryReader | Uint8Array, length?: number): AlignmentState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlignmentState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.enabled = reader.bool();
          break;
        case 2:
          message.lastObservationHeight = reader.uint64();
          break;
        case 3:
          message.observationCount = reader.uint64();
          break;
        case 4:
          message.degradedFrequencyActive = reader.bool();
          break;
        case 5:
          message.previousCategory = reader.string();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromPartial(object: DeepPartial<AlignmentState>): AlignmentState {
    const message = createBaseAlignmentState();
    message.enabled = object.enabled ?? false;
    message.lastObservationHeight = object.lastObservationHeight !== undefined && object.lastObservationHeight !== null ? BigInt(object.lastObservationHeight.toString()) : BigInt(0);
    message.observationCount = object.observationCount !== undefined && object.observationCount !== null ? BigInt(object.observationCount.toString()) : BigInt(0);
    message.degradedFrequencyActive = object.degradedFrequencyActive ?? false;
    message.previousCategory = object.previousCategory ?? "";
    return message;
  }
};
function createBaseAlignmentObservation(): AlignmentObservation {
  return {
    height: BigInt(0),
    timestamp: BigInt(0),
    knowledgeQuality: BigInt(0),
    economicStability: BigInt(0),
    governanceParticipation: BigInt(0),
    networkSecurity: BigInt(0),
    stakingRatio: BigInt(0)
  };
}
/**
 * AlignmentObservation records sensor readings at a given height.
 * @name AlignmentObservation
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.AlignmentObservation
 */
export const AlignmentObservation = {
  typeUrl: "/zerone.alignment.v1.AlignmentObservation",
  encode(message: AlignmentObservation, writer: BinaryWriter = BinaryWriter.create()): BinaryWriter {
    if (message.height !== BigInt(0)) {
      writer.uint32(8).uint64(message.height);
    }
    if (message.timestamp !== BigInt(0)) {
      writer.uint32(16).int64(message.timestamp);
    }
    if (message.knowledgeQuality !== BigInt(0)) {
      writer.uint32(24).uint64(message.knowledgeQuality);
    }
    if (message.economicStability !== BigInt(0)) {
      writer.uint32(32).uint64(message.economicStability);
    }
    if (message.governanceParticipation !== BigInt(0)) {
      writer.uint32(40).uint64(message.governanceParticipation);
    }
    if (message.networkSecurity !== BigInt(0)) {
      writer.uint32(48).uint64(message.networkSecurity);
    }
    if (message.stakingRatio !== BigInt(0)) {
      writer.uint32(56).uint64(message.stakingRatio);
    }
    return writer;
  },
  decode(input: BinaryReader | Uint8Array, length?: number): AlignmentObservation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlignmentObservation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.height = reader.uint64();
          break;
        case 2:
          message.timestamp = reader.int64();
          break;
        case 3:
          message.knowledgeQuality = reader.uint64();
          break;
        case 4:
          message.economicStability = reader.uint64();
          break;
        case 5:
          message.governanceParticipation = reader.uint64();
          break;
        case 6:
          message.networkSecurity = reader.uint64();
          break;
        case 7:
          message.stakingRatio = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromPartial(object: DeepPartial<AlignmentObservation>): AlignmentObservation {
    const message = createBaseAlignmentObservation();
    message.height = object.height !== undefined && object.height !== null ? BigInt(object.height.toString()) : BigInt(0);
    message.timestamp = object.timestamp !== undefined && object.timestamp !== null ? BigInt(object.timestamp.toString()) : BigInt(0);
    message.knowledgeQuality = object.knowledgeQuality !== undefined && object.knowledgeQuality !== null ? BigInt(object.knowledgeQuality.toString()) : BigInt(0);
    message.economicStability = object.economicStability !== undefined && object.economicStability !== null ? BigInt(object.economicStability.toString()) : BigInt(0);
    message.governanceParticipation = object.governanceParticipation !== undefined && object.governanceParticipation !== null ? BigInt(object.governanceParticipation.toString()) : BigInt(0);
    message.networkSecurity = object.networkSecurity !== undefined && object.networkSecurity !== null ? BigInt(object.networkSecurity.toString()) : BigInt(0);
    message.stakingRatio = object.stakingRatio !== undefined && object.stakingRatio !== null ? BigInt(object.stakingRatio.toString()) : BigInt(0);
    return message;
  }
};
function createBaseDimensionScores(): DimensionScores {
  return {
    height: BigInt(0),
    knowledgeQuality: BigInt(0),
    economicStability: BigInt(0),
    governanceParticipation: BigInt(0),
    networkSecurity: BigInt(0),
    stakingRatio: BigInt(0),
    composite: BigInt(0)
  };
}
/**
 * DimensionScores holds weighted and composite scores for a height.
 * @name DimensionScores
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.DimensionScores
 */
export const DimensionScores = {
  typeUrl: "/zerone.alignment.v1.DimensionScores",
  encode(message: DimensionScores, writer: BinaryWriter = BinaryWriter.create()): BinaryWriter {
    if (message.height !== BigInt(0)) {
      writer.uint32(8).uint64(message.height);
    }
    if (message.knowledgeQuality !== BigInt(0)) {
      writer.uint32(16).uint64(message.knowledgeQuality);
    }
    if (message.economicStability !== BigInt(0)) {
      writer.uint32(24).uint64(message.economicStability);
    }
    if (message.governanceParticipation !== BigInt(0)) {
      writer.uint32(32).uint64(message.governanceParticipation);
    }
    if (message.networkSecurity !== BigInt(0)) {
      writer.uint32(40).uint64(message.networkSecurity);
    }
    if (message.stakingRatio !== BigInt(0)) {
      writer.uint32(48).uint64(message.stakingRatio);
    }
    if (message.composite !== BigInt(0)) {
      writer.uint32(56).uint64(message.composite);
    }
    return writer;
  },
  decode(input: BinaryReader | Uint8Array, length?: number): DimensionScores {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDimensionScores();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.height = reader.uint64();
          break;
        case 2:
          message.knowledgeQuality = reader.uint64();
          break;
        case 3:
          message.economicStability = reader.uint64();
          break;
        case 4:
          message.governanceParticipation = reader.uint64();
          break;
        case 5:
          message.networkSecurity = reader.uint64();
          break;
        case 6:
          message.stakingRatio = reader.uint64();
          break;
        case 7:
          message.composite = reader.uint64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromPartial(object: DeepPartial<DimensionScores>): DimensionScores {
    const message = createBaseDimensionScores();
    message.height = object.height !== undefined && object.height !== null ? BigInt(object.height.toString()) : BigInt(0);
    message.knowledgeQuality = object.knowledgeQuality !== undefined && object.knowledgeQuality !== null ? BigInt(object.knowledgeQuality.toString()) : BigInt(0);
    message.economicStability = object.economicStability !== undefined && object.economicStability !== null ? BigInt(object.economicStability.toString()) : BigInt(0);
    message.governanceParticipation = object.governanceParticipation !== undefined && object.governanceParticipation !== null ? BigInt(object.governanceParticipation.toString()) : BigInt(0);
    message.networkSecurity = object.networkSecurity !== undefined && object.networkSecurity !== null ? BigInt(object.networkSecurity.toString()) : BigInt(0);
    message.stakingRatio = object.stakingRatio !== undefined && object.stakingRatio !== null ? BigInt(object.stakingRatio.toString()) : BigInt(0);
    message.composite = object.composite !== undefined && object.composite !== null ? BigInt(object.composite.toString()) : BigInt(0);
    return message;
  }
};
function createBaseCorrectionRecord(): CorrectionRecord {
  return {
    height: BigInt(0),
    dimension: "",
    parameter: "",
    direction: "",
    magnitude: BigInt(0),
    applied: false,
    timestamp: BigInt(0)
  };
}
/**
 * CorrectionRecord logs a correction generated by alignment analysis.
 * @name CorrectionRecord
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.CorrectionRecord
 */
export const CorrectionRecord = {
  typeUrl: "/zerone.alignment.v1.CorrectionRecord",
  encode(message: CorrectionRecord, writer: BinaryWriter = BinaryWriter.create()): BinaryWriter {
    if (message.height !== BigInt(0)) {
      writer.uint32(8).uint64(message.height);
    }
    if (message.dimension !== "") {
      writer.uint32(18).string(message.dimension);
    }
    if (message.parameter !== "") {
      writer.uint32(26).string(message.parameter);
    }
    if (message.direction !== "") {
      writer.uint32(34).string(message.direction);
    }
    if (message.magnitude !== BigInt(0)) {
      writer.uint32(40).uint64(message.magnitude);
    }
    if (message.applied === true) {
      writer.uint32(48).bool(message.applied);
    }
    if (message.timestamp !== BigInt(0)) {
      writer.uint32(56).int64(message.timestamp);
    }
    return writer;
  },
  decode(input: BinaryReader | Uint8Array, length?: number): CorrectionRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCorrectionRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.height = reader.uint64();
          break;
        case 2:
          message.dimension = reader.string();
          break;
        case 3:
          message.parameter = reader.string();
          break;
        case 4:
          message.direction = reader.string();
          break;
        case 5:
          message.magnitude = reader.uint64();
          break;
        case 6:
          message.applied = reader.bool();
          break;
        case 7:
          message.timestamp = reader.int64();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromPartial(object: DeepPartial<CorrectionRecord>): CorrectionRecord {
    const message = createBaseCorrectionRecord();
    message.height = object.height !== undefined && object.height !== null ? BigInt(object.height.toString()) : BigInt(0);
    message.dimension = object.dimension ?? "";
    message.parameter = object.parameter ?? "";
    message.direction = object.direction ?? "";
    message.magnitude = object.magnitude !== undefined && object.magnitude !== null ? BigInt(object.magnitude.toString()) : BigInt(0);
    message.applied = object.applied ?? false;
    message.timestamp = object.timestamp !== undefined && object.timestamp !== null ? BigInt(object.timestamp.toString()) : BigInt(0);
    return message;
  }
};
function createBaseAlignmentHealthIndex(): AlignmentHealthIndex {
  return {
    height: BigInt(0),
    compositeScore: BigInt(0),
    category: "",
    dimensionalScores: undefined,
    correctionsGenerated: 0
  };
}
/**
 * AlignmentHealthIndex is the composite health metric for a given height.
 * @name AlignmentHealthIndex
 * @package zerone.alignment.v1
 * @see proto type: zerone.alignment.v1.AlignmentHealthIndex
 */
export const AlignmentHealthIndex = {
  typeUrl: "/zerone.alignment.v1.AlignmentHealthIndex",
  encode(message: AlignmentHealthIndex, writer: BinaryWriter = BinaryWriter.create()): BinaryWriter {
    if (message.height !== BigInt(0)) {
      writer.uint32(8).uint64(message.height);
    }
    if (message.compositeScore !== BigInt(0)) {
      writer.uint32(16).uint64(message.compositeScore);
    }
    if (message.category !== "") {
      writer.uint32(26).string(message.category);
    }
    if (message.dimensionalScores !== undefined) {
      DimensionScores.encode(message.dimensionalScores, writer.uint32(34).fork()).ldelim();
    }
    if (message.correctionsGenerated !== 0) {
      writer.uint32(40).uint32(message.correctionsGenerated);
    }
    return writer;
  },
  decode(input: BinaryReader | Uint8Array, length?: number): AlignmentHealthIndex {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAlignmentHealthIndex();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.height = reader.uint64();
          break;
        case 2:
          message.compositeScore = reader.uint64();
          break;
        case 3:
          message.category = reader.string();
          break;
        case 4:
          message.dimensionalScores = DimensionScores.decode(reader, reader.uint32());
          break;
        case 5:
          message.correctionsGenerated = reader.uint32();
          break;
        default:
          reader.skipType(tag & 7);
          break;
      }
    }
    return message;
  },
  fromPartial(object: DeepPartial<AlignmentHealthIndex>): AlignmentHealthIndex {
    const message = createBaseAlignmentHealthIndex();
    message.height = object.height !== undefined && object.height !== null ? BigInt(object.height.toString()) : BigInt(0);
    message.compositeScore = object.compositeScore !== undefined && object.compositeScore !== null ? BigInt(object.compositeScore.toString()) : BigInt(0);
    message.category = object.category ?? "";
    message.dimensionalScores = object.dimensionalScores !== undefined && object.dimensionalScores !== null ? DimensionScores.fromPartial(object.dimensionalScores) : undefined;
    message.correctionsGenerated = object.correctionsGenerated ?? 0;
    return message;
  }
};